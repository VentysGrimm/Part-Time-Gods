# Foundry VTT v14 Production Smoke Test

Status: Runtime QA in progress. Issue #131 remains open.

Updated: 2026-07-12
System: Part-Time Gods 2E 0.1.0
Repository: VentysGrimm/Part-Time-Gods
OS: Windows, user data under `C:\Users\Owner\AppData\Local\FoundryVTT`
Foundry target: v14 only

## Environment

| Field | Result |
| --- | --- |
| Foundry version | Pass. Status API reports Foundry `14.364`. |
| Browser | Pass. Codex in-app browser verified setup/join pages; live desktop Foundry window was used for the GM runtime continuation. |
| QA world | Pass. Status API reports active world `ptg-v14-qa-smoke-test`, system `part-time-gods`, system version `0.1.0`. |
| System install from local checkout | Pass. The running QA world is using this checkout at `C:\Users\Owner\AppData\Local\FoundryVTT\Data\systems\part-time-gods`. |
| System install from public manifest/latest release | Pending 0.1.0 release publication and Foundry installer UI test. The live release manifest is reachable but still advertises 0.0.2; the 0.1.0 ZIP asset is not published yet. |
| Screenshots | Not captured in this pass. Browser-visible UI and chat-card evidence recorded below. |

## Preflight Evidence

| Check | Result | Notes |
| --- | --- | --- |
| JavaScript syntax check | Pass | 2026-07-11: `node --input-type=module --eval "await import('./scripts/check-syntax.mjs')"` reported 61 files checked. Direct script-runner paths can hit sandbox `EPERM` on `C:\Users\Owner\AppData\Local\FoundryVTT\Data`. |
| Release validation | Pass | 2026-07-12: `npm.cmd run validate` reported 62 actors, 731 premade items, 40 choices, 9 journals, 91 roll tables, 1 scene, 8 macros, and stable source keys. |
| Premade Item source audit | Pass | 2026-07-12: `node scripts\audit-premade-items.mjs` passed outside the sandbox after the expected parent-path `EPERM`; 771 created Item docs, 0 issue buckets. |
| Automated tests | Pass | 2026-07-12: `node scripts\run-tests.mjs` passed outside the sandbox after the expected Foundry data-path `EPERM` in sandboxed execution: 54 tests, 54 pass, 0 fail. |
| Release ZIP build | Pending rebuild after Foundry closes | `system.json` now targets `dist\part-time-gods-0.1.0.zip`; Foundry is currently running and all active pack LevelDB folders have `LOCK` files, so rebuild the ZIP after Foundry releases the live pack files. |
| GitHub Actions | Pass | Main branch validation for the drop-data and initiative coverage slice passed at `91094ebd90f293cb4f516e323f3bb3a27aee3152`: `https://github.com/VentysGrimm/Part-Time-Gods/actions/runs/28949168516`. |
| Public manifest install path | Pending 0.1.0 release asset update | `https://github.com/VentysGrimm/Part-Time-Gods/releases/latest/download/system.json` returned HTTP 200 on 2026-07-11, but the live asset still advertises version `0.0.2` and download `v0.0.2/part-time-gods-0.0.2.zip`. |
| Public ZIP install path | Pending for 0.1.0 release asset | `https://github.com/VentysGrimm/Part-Time-Gods/releases/download/v0.1.0/part-time-gods-0.1.0.zip` returned HTTP 404 on 2026-07-11 and must be published before this check can pass for the new version. |

## Required Runtime Flow

| Test | Status | Result / Notes |
| --- | --- | --- |
| Install system from local checkout | Pass | Running Foundry v14.364 world is using this checkout. |
| Install system from release ZIP or manifest | Pending release publication and runtime install test | The live latest-release manifest currently advertises `0.0.2`, and the `0.1.0` ZIP asset returns HTTP 404. Foundry's install-by-manifest UI still needs a separate clean install target after the 0.1.0 release assets are published. |
| Create a fresh world using the system | Pass | Active QA world is `ptg-v14-qa-smoke-test`. |
| Create Character actor | Pass | `QA Character` exists and was used for sheet, creator, roll, damage, condition, and recovery tests. |
| Create Antagonist actor | Pass | `QA Antagonist` exists. |
| Create Pantheon actor | Pass | `QA Pantheon` exists; Pantheon sheet showed Table Tools in prior runtime pass. |
| Populate compendia | Pass | Runtime notification previously reported `Updated 152 Part-Time Gods compendium entries.` |
| Create/open territory scene | Pass | GM setup created/opened `God Territory Grid`; Territory Grid app showed 10x10 grid, 0 points, and 0 influenced cells. |
| Use character creator with manual choices | Pass | Character Creator completed with valid budgets: Skill Points 10/10, Manifestation Points 4/4, Attachment Points 5/5, Spark 1, Fragments 3, Starting Truth `QA Truth`. Creator, validation, and apply-preview popouts now use readable sheet-dialog styling with opaque paper backgrounds. |
| Use Random God helper | Pass | Random God helper populated source-backed selections including Occupation, Archetype, Dominion, Theology, Blessing, and Curse before final apply. |
| Drag/drop Occupation item | Pending live proof | Drag/drop matrix not yet run live. Automated coverage now verifies shared Item drop-data resolution for UUID, compendium pack, world Item, and embedded data sources, plus character sheet `_onDrop` copying the full #131 item matrix into actor inventory. |
| Drag/drop Archetype item | Pending live proof | Drag/drop matrix not yet run live. Automated coverage now verifies shared Item drop-data resolution for UUID, compendium pack, world Item, and embedded data sources, plus character sheet `_onDrop` copying the full #131 item matrix into actor inventory. |
| Drag/drop Dominion item | Pending live proof | Drag/drop matrix not yet run live. Automated coverage now verifies shared Item drop-data resolution for UUID, compendium pack, world Item, and embedded data sources, plus character sheet `_onDrop` copying the full #131 item matrix into actor inventory. |
| Drag/drop Theology item | Pending live proof | Drag/drop matrix not yet run live. Automated coverage now verifies shared Item drop-data resolution for UUID, compendium pack, world Item, and embedded data sources, plus character sheet `_onDrop` copying the full #131 item matrix into actor inventory. |
| Drag/drop Blessing item | Pending live proof | Drag/drop matrix not yet run live. Automated coverage now verifies shared Item drop-data resolution for UUID, compendium pack, world Item, and embedded data sources, plus character sheet `_onDrop` copying the full #131 item matrix into actor inventory. |
| Drag/drop Curse item | Pending live proof | Drag/drop matrix not yet run live. Automated coverage now verifies shared Item drop-data resolution for UUID, compendium pack, world Item, and embedded data sources, plus character sheet `_onDrop` copying the full #131 item matrix into actor inventory. |
| Drag/drop Truth item | Pending live proof | Drag/drop matrix not yet run live. Automated coverage now verifies shared Item drop-data resolution for UUID, compendium pack, world Item, and embedded data sources, plus character sheet `_onDrop` copying the full #131 item matrix into actor inventory. |
| Drag/drop Relic item | Pending live proof | Drag/drop matrix not yet run live. Automated coverage now verifies shared Item drop-data resolution for UUID, compendium pack, world Item, and embedded data sources, plus character sheet `_onDrop` copying the full #131 item matrix into actor inventory. |
| Drag/drop Bond item | Pending live proof | Drag/drop matrix not yet run live. Automated coverage now verifies shared Item drop-data resolution for UUID, compendium pack, world Item, and embedded data sources, plus character sheet `_onDrop` copying the full #131 item matrix into actor inventory. |
| Drag/drop Worshipper item | Pending live proof | Drag/drop matrix not yet run live. Automated coverage now verifies shared Item drop-data resolution for UUID, compendium pack, world Item, and embedded data sources, plus character sheet `_onDrop` copying the full #131 item matrix into actor inventory. |
| Drag/drop Vassal item | Pending live proof | Drag/drop matrix not yet run live. Automated coverage now verifies shared Item drop-data resolution for UUID, compendium pack, world Item, and embedded data sources, plus character sheet `_onDrop` copying the full #131 item matrix into actor inventory. |
| Drag/drop Condition item | Pending live proof | Drag/drop matrix not yet run live. Automated coverage now verifies shared Item drop-data resolution for UUID, compendium pack, world Item, and embedded data sources, plus character sheet `_onDrop` copying the full #131 item matrix into actor inventory. |
| Drag/drop Weapon item | Pending live proof | Drag/drop matrix not yet run live. Automated coverage now verifies shared Item drop-data resolution for UUID, compendium pack, world Item, and embedded data sources, plus character sheet `_onDrop` copying the full #131 item matrix into actor inventory. |
| Drag/drop Armor item | Pending live proof | Drag/drop matrix not yet run live. Automated coverage now verifies shared Item drop-data resolution for UUID, compendium pack, world Item, and embedded data sources, plus character sheet `_onDrop` copying the full #131 item matrix into actor inventory. |
| Roll Skill Combo | Pass | Standard zero-pool Skill Combo posted a Fate Die card. Opposed and extended checks also posted chat cards. |
| Roll Manifestation | Pass | `QA Character: Aegis + Discipline` posted Base Pool 5d10, Successes 3, Difficulty 1, Margin 2, Success. |
| Roll opposed check | Pass | `QA Character: Athletics + Athletics`, Mode Opposed, Base Pool 6d10, Successes 1 vs Difficulty 2, Failure. |
| Roll extended check | Pass | `QA Character: Athletics + Athletics`, Mode Extended, Base Pool 6d10, Successes 3, Extended Progress 5/6, In Progress, Success. |
| Apply Health damage | Pass | Chat card posted `Health Damage`, Raw Damage 2, Applied 2, `Health: 8 -> 6`. |
| Apply Psyche damage | Pass | Chat card posted `Psyche Damage`, Raw Damage 2, Applied 2, `Psyche: 10 -> 8`. |
| Apply armor | Partial | Health damage dialog exposed and used the armor toggle, but `QA Character` had no equipped armor in the live pass. 2026-07-08 desktop continuation created `QA Armor Live Proof` as an Armor item, set Rating 2, and checked Equipped, but sidebar-to-sheet drop attempts did not attach the item to `QA Character` under automation. Automated coverage exercises `applyDamageToActor()` with equipped armor plus a matching proof quality and verifies sheet `_onDrop` copies Armor drops into actor gear; live equipped-armor reduction still needs coverage. |
| Apply Conditions | Pass | Chat card added custom condition `QA Smoke Strain`, category physical, severity 1, to `QA Character`. |
| Recover/reduce Conditions | Pass | `Reduce` removed `QA Smoke Strain`. Explicit `Recover` on `QA Recover Check` rolled Medicine + Empathy and posted `Severity: 1 -> 0`, Outcome Removed. |
| Apply healing | Pass | Character sheet resource restore increased Health from 6/8 to 7/8. Combat-control healing has automated coverage for a selected combatant actor, but live proof still needs an encounter with actual actor combatants. |
| Open Combat Controls from GM panel or scene controls | Partial | GM setup panel `Combat Controls` correctly warned before an encounter existed. In the active encounter retest, Combat Controls initially failed because v14 collection-backed `combat.combatants` was treated as an array; this was fixed in `module/combat/ptg-combat.mjs`, Foundry was reloaded, and Combat Controls opened and posted a `Round and Turn Sequence` card. 2026-07-08 desktop continuation verified the `PTG GM Setup` scene control opens `PTG Combat Controls`; chat-command aliases are not part of the desired workflow. The live Actor/Attacker dropdown currently offers only `Encounter helper only`, so combatant-specific live action markers and combat-control healing still need coverage after adding actors to the encounter. Automated coverage verifies collection-backed combatants render in the dialog, `rollPTGInitiative()` updates collection-backed combatants, selected-combatant action markers are recorded, combat-control healing updates the actor resource plus chat card, and the native Combat Tracker initiative roll path uses the same PTG initiative formula. Live tracker proof remains due after actors are added to the encounter. |
| Open Territory Grid from GM panel or scene controls | Pass | GM setup and scene control paths opened Territory Grid tooling and the God Territory Grid scene. |
| Open Mortal-Divine Balance from GM panel | Pass | GM setup opened Mortal-Divine Balance tracker with `QA Character - Balanced (0)`. |
| Open Opposition Builder from GM panel | Pass | GM setup opened PTG Opposition Builder and created `QA Builder Antagonist`; the created antagonist sheet rendered with builder notes. |
| Test GM permission boundary | Partial | 2026-07-11 browser continuation rejoined the active world as `Player2` at `/game` and verified the visible non-GM UI does not expose `PTG GM Setup`, `Combat Controls`, `Territory Controls`, `Opposition Builder`, or `Mortal-Divine Balance` controls. Automated coverage verifies non-GM users cannot open Combat Controls. Remaining live role cases still need owner/observer/non-owner action checks. |
| Test owner permission boundary | Pending live matrix | Non-GM owner account/path not yet tested live. Automated coverage verifies non-owner, non-GM users cannot apply Health damage to an actor. |
| Test observer permission boundary | Pending | Observer account/path not yet tested. |
| Test non-owner permission boundary | Pending | Non-owner account/path not yet tested. |
| Test migration against existing world | Pending live snapshot | Automated coverage now verifies legacy attachment text is preserved in actor flags and legacy sheet notes are converted into owned Items without duplicating already migrated fields. A real existing-world snapshot still has not been run. |

## Runtime Evidence Notes

- `QA Character` creator apply path persisted Occupation `Medical - Therapist`, Archetype `The Wanderer`, Dominion `QA God of Smoke Tests`, Theology `Cult of the Saints`, Health 8, Psyche 10, Free Time 4, Wealth 5, and Fragments 3 before later damage tests.
- Character Creator popouts were tightened after the desktop continuation exposed poor readability: the main creator, validation, and apply-confirm dialogs now share the readable `ptg-sheet-dialog` shell and explicit paper backgrounds.
- 2026-07-08 desktop continuation reloaded Foundry and visually verified the `QA Character` Creator popup and Character Creator Validation popup render with opaque paper backgrounds, readable dark text, and clear purple action buttons.
- 2026-07-11 source-side readability continuation: the in-app browser reopened `QA Character` at a Foundry-safe viewport and confirmed the printed title rows keep `Name:` and `God/dess Of:` labels plus their input boxes inside 44px-high containers with white input backgrounds and black text. A direct browser click opened the `QA Character: Add Condition` dialog and exposed a remaining plain Foundry `application dialog` case where PTG CSS variables were not inherited by select/input controls; stylesheet fallbacks now give PTG dialog fields concrete white backgrounds, black text, bounded widths, and internal overflow handling even when the outer dialog lacks the `part-time-gods` class.
- Manifestation Measures posted `Aegis Measures` with 3 successes available, 1 measure spent on Damage, 2 unspent successes, and QA notes.
- Condition recovery posted both a recovery roll card and a condition recovery summary card.
- Opposition Builder generated a real world Actor instead of only opening the dialog.
- Active-combat retest created and started Combat encounter 1, then verified the GM panel `Combat Controls` dialog opened after the v14 combatants collection fix and posted `Round and Turn Sequence` to chat.
- Shared drop-data resolver coverage verifies Item drops from UUID, compendium pack, world Item, and embedded data payloads, which supports the actor-sheet drag/drop matrix but does not replace the required live sheet drop pass.
- Character-sheet drop coverage verifies `_onDrop` copies the full #131 item matrix into actor inventory, applies core choice drops through `actor.applyChoice()`, and rejects an Armor item dropped into a typed Truth section.
- Combat-control coverage verifies dialog option rendering, initiative updates, selected-combatant action markers, and combat-control healing for Foundry v14 collection-backed combatants.
- Permission-boundary coverage now verifies non-GM Combat Controls denial and non-owner damage denial in automated tests; owner, observer, and non-owner role behavior still need live Foundry verification.
- Migration coverage verifies both existing-world safety paths: schema migration preserves legacy attachment text in actor flags, and canonical embedded Item migration converts legacy condition/relic sheet notes into owned Items while skipping a duplicate already migrated bond field.
- 2026-07-08 desktop continuation: Foundry v14.364 reported active world `ptg-v14-qa-smoke-test`, system `part-time-gods` 0.0.2, and one connected user. GM Setup opened from the Part-Time Gods scene controls, and `Combat Controls` opened the `PTG Combat Controls` dialog. GM panel controls are the supported runtime path for this pass; chat-command aliases are intentionally out of scope.
- 2026-07-11 version alignment: local Foundry status API reported active world `ptg-v14-qa-smoke-test`, system `part-time-gods`, system version `0.1.0`; package, manifest, validation, and release ZIP references were aligned to `0.1.0`. A follow-up live release check found the latest GitHub Release `system.json` asset is still `0.0.2` and the advertised `v0.1.0/part-time-gods-0.1.0.zip` asset returns HTTP 404, so the public install path remains pending release asset publication.
- 2026-07-12 status refresh: `http://127.0.0.1:30000/api/status` returned Foundry `14.364`, active world `ptg-v14-qa-smoke-test`, system `part-time-gods`, system version `0.1.0`, and 1 connected user. The local checkout still has live LevelDB pack locks, so source-side validation can continue but pack rebuild/release ZIP work should wait until Foundry is closed.
- 2026-07-12 browser refresh: the in-app browser joined `/game` as `Player2` without a password, but the visible roster showed `Player2 [GM]`, so this route cannot prove non-GM/player-only behavior. At the default 1280x720 viewport Foundry displayed its minimum-size warning; a temporary 1280x900 viewport removed that warning. The first `/game` load briefly showed the integrated `PTG Territory Grid` window with `GM Controls`, `View`, `Move`, `Background`, `Import`, `Clear`, and `Refresh`, but after reload at the supported viewport the Territory Grid window was not open, so startup/resize proof remains incomplete.
- 2026-07-12 premade Item source audit: `scripts/audit-premade-items.mjs` now audits both `character-creation` and `premade-items` source collections. It reported 771 created Item docs, 40 character-creation Items, 731 premade Items, and zero invalid types, invalid folders, duplicate type/name pairs, duplicate source IDs, journal-style Item kinds, `journal:` source IDs, or missing source IDs. Shipped/live pack verification remains pending until the LevelDB pack churn is intentionally reconciled.
- 2026-07-12 Skill Combo dialog source slice: the Skill Combo Check dialog now has a bounded flex chain plus a content-based `:has(.ptg-skill-combo-options)` fallback so the options body scrolls internally while the footer remains usable. Release validation and the full Node harness passed after the expected sandbox `EPERM` rerun outside the sandbox. Live visual proof in Foundry remains useful but no source-side blocker remains for that dialog.
- 2026-07-11 Territory integration source slice: Territory now registers an integrated world-ready auto-open setting, scene control visibility for GMs or worlds with a Territory scene, a resizable/scrollable GM interface, GM-only edit/create/import/background/clear paths, Character actor drop import for territory attachments, player routing to the Territory Scene, and fit-to-canvas scene viewing. The point model stores rulebook-facing location type, control source, boundary/status, discovery state, public notes, GM secrets, footprint, Dominion/theology tags, linked Bond/Actor/Item UUIDs, and ritual/event state while player-facing context filters hidden points and GM-only details. Validation passed with `npm.cmd run validate`, source-data Territory tests passed, and the full Node harness passed after the expected sandbox `EPERM` rerun outside the sandbox: 30 tests, 30 pass. Live browser proof joined `/game` after selecting `Player2`; the integrated Territory Grid window auto-opened and showed `God Territory Grid`, `GM Controls`, 3 points, and 2 influenced cells. This proves the earlier world-ready GM/interface startup path, but not the revised player Territory Scene route or GM background/drop controls live.
- 2026-07-12 Territory scene label source slice: the managed `Legend` drawing is inset inside the top-left label cell and uses a smaller text box/font so it no longer shares the full corner cell with the row/column number layout. Source-data tests now assert the Legend box ends before the row-1 and column-1 label cells. Live scene regeneration/visual QA remains pending because `packs/maps` is locked while Foundry is running.
- 2026-07-12 Mortal-Divine tracker source slice: the tracker now has a world-ready auto-open setting, GM party-style tracked roster persistence, Character actor dropdown/add/remove controls, Character actor drop import, a read-only player view limited to owned character balance bars, explicit resize/scroll CSS for GM and player modes, container-width responsive layout, and an internally scrolling GM party roster. Automated source-data coverage verifies tracked UUID normalization, GM roster visibility, player owned-character filtering, and fallback player-owned display. Live GM/player startup and resize proof still need a Foundry browser pass.
- 2026-07-11 Item sheet readability source slice: shared Item sheet fields now enforce readable input/select/textarea height, padding, and line-height across the active `PTGItemSheet` path and the legacy Domain/Power fallback templates; rich text Item sections are now open-by-default collapsible panels so dense sheets can be compressed instead of clipping labels or field rows. Release validation guards enforce the shared item field geometry and collapsible editor markup. Live visual confirmation across representative Item types still needs a Foundry browser pass.
- 2026-07-11 edit-lock source slice: Character, Antagonist, Pantheon, and Item sheets now receive a shared permission-aware edit-lock strip, open in protected mode for users with edit permission, disable fields/destructive buttons/drop edits while locked, and expose explicit unlock/lock behavior through `module/sheets/sheet-edit-lock.mjs`. Unit coverage verifies default locked state, unlock toggling, and permission denial; release validation guards the lock helper, lock controls, and locked-state CSS. Live visual/user-permission proof remains required.
- 2026-07-11 Character ability readability source slice: Character sheet Blessing/Curse/Relic/Truth detail rows now use structured label/body containers, compact action groups, and a shared detail formatter that decodes escaped simple HTML into readable prose while stripping unsafe or layout-breaking markup. Unit coverage verifies escaped HTML, plain angle-bracket text, and unsafe markup handling; release validation guards the detail formatter, action groups, and detail body CSS. Live visual proof against a long Blessing such as Borrowed Lives remains required.
- 2026-07-08 browser continuation joined the same active world through `http://127.0.0.1:30000/join` as `Player2`; the live page reached `/game`, showed `Player2`, and the visible non-GM UI did not contain `PTG GM Setup`, `Combat Controls`, `Territory Controls`, `Opposition Builder`, or `Mortal-Divine Balance`. Generic Foundry controls, chat cards, and sidebar tabs were visible. A browser click attempt against old chat-card `Health Damage` buttons could not be completed because the in-app browser click translation missed the Foundry sidebar coordinate, so live non-owner damage denial remains unresolved.
- 2026-07-11 browser continuation again joined the active world as `Player2`; the live DOM reached `/game`, showed `Player2` and `Gamemaster [GM]` connected, and did not show `PTG GM Setup`, `Combat Controls`, `Territory Controls`, `Opposition Builder`, or `Mortal-Divine Balance` in the visible non-GM UI. The old chat-card `Health Damage` buttons were visible in the DOM, but in-app browser coordinate translation still missed the Foundry sidebar target, so non-owner damage denial remains covered by automated tests but not live-click proven.
- The live `PTG Combat Controls` Actor/Attacker selector exposed only `Encounter helper only` during this continuation. The God Territory Grid scene was viewed and its 10x10 grid was visible, but actor-sidebar token placement did not create tokens under automation, so action marker and combat-control healing proof still require adding actual actor combatants to the encounter.
- 2026-07-08 desktop continuation: `QA Armor Live Proof` was created through the Items sidebar as an Armor item, configured to Rating 2, and marked Equipped. Repeated sidebar-to-sheet drag/drop attempts from the item icon and item row text did not attach it to `QA Character` in the automated desktop session; this leaves live drag/drop and equipped-armor UI proof unresolved even though direct sheet-drop handler coverage passes.
- 2026-07-08 maps-pack audit: copied `packs/maps` without the live LevelDB `LOCK` file and inspected it with `classic-level`. The shipped pack contained 1 Scene, 24 Drawing child rows, and 24 stale Drawing child rows with empty `author` and `shape.type` set to `rectangle`; the active world scene copy was already clean, so the defect is isolated to stale compendium pack data. Source generation now emits fallback author `0000000000000000` and v14 rectangle shape code `r`, release validation audits premade Scene drawing schema, and automated source-data tests verify both v14-ready source drawings and stale managed Drawing refresh behavior. Rebuilding the actual `packs/maps` LevelDB remains pending until Foundry is closed and releases the pack lock.

## Unresolved Coverage Gaps

| Gap | Impact | Next Action |
| --- | --- | --- |
| Foundry install-by-manifest UI has not been run in a clean install target. | The live GitHub Release manifest is reachable but still advertises `0.0.2`, and the `0.1.0` ZIP asset is missing. The Foundry installer path itself is not proven for `0.1.0`. | After closing Foundry, rebuild packs and `dist/part-time-gods-0.1.0.zip`, publish `dist/system.json` plus the ZIP to the `v0.1.0` GitHub Release, then use a separate Foundry data path or temporarily remove the local system, install from the GitHub Release manifest URL, and open a world. |
| Premade Items compendium needs shipped/live pack verification. | Source-data audit now proves all created Item source docs are true Items with valid types/folders/source IDs and no journal-style leaks, but the live/generated LevelDB pack files have not been reconciled because Foundry is running with pack locks. | Close Foundry, rebuild packs, audit the generated `premade-items` and `character-creation` packs, then verify the live compendium contains only valid Item content before closing #172. |
| Drag/drop item matrix is untested live. | Shared drop-data resolution is covered for Foundry Item source shapes, and the character sheet `_onDrop` path is covered for every item type named in the #131 drag/drop matrix plus typed-section rejection, but owned Item sheet drop behavior across the main player-facing item types is not proven in the browser. | Drag/drop Occupation, Archetype, Dominion, Theology, Blessing, Curse, Truth, Relic, Bond, Worshipper, Vassal, Condition, Weapon, and Armor onto a character and record the resulting owned Items. |
| Equipped armor reduction has automated coverage but still needs live UI proof. | The armor toggle exists, `applyDamageToActor()` is covered for equipped armor plus matching proof quality, and sheet `_onDrop` is covered for Armor gear as part of the full #131 item matrix. The live actor sheet/dialog path is not proven with equipped armor on `QA Character` because automated desktop sidebar-to-sheet drops did not attach `QA Armor Live Proof`. | Manually confirm the sidebar-to-sheet Armor drop or use a reliable live UI route to equip armor on `QA Character`, apply Health damage with armor enabled, and verify reduced Applied damage in Foundry chat. |
| Active combat helper flow is partially tested. | Combat Controls now opens from GM Setup and posts an encounter helper card in an active encounter, and automated tests cover collection-backed dialog rendering, initiative updates, selected-combatant action markers, and combat-control healing. The live Actor/Attacker selector had no actor combatants available in the 2026-07-08 desktop continuation, so action-state markers and combat-control healing are not live-proven. | Add QA Character and QA Antagonist tokens to the Combat encounter, confirm they appear in the Combat Controls Actor/Attacker selector, then run initiative, damage/healing, and action-state posts through Combat Controls from the GM panel path. |
| Permission boundaries are partially live-proven. | Automated tests verify non-GM Combat Controls denial and non-owner damage denial. Live browser continuation verified `Player2` does not see the PTG GM setup/control surfaces, but owner, observer, and non-owner action denial still need live proof. | Configure or join test users with owner, observer, and non-owner states, then verify visible controls and denied actions without chat commands. |
| Territory player scene and GM background/drop controls need live proof. | Source and validator coverage now enforce the player Territory Scene route, fit-to-canvas math, GM-only mutation guards, Character actor import, background update payloads, and non-overlapping Legend geometry. The 2026-07-12 browser pass could join as `Player2`, but the visible roster showed `Player2 [GM]`; this proves the current route is not a usable non-GM player-route test. | Reconfigure or add a true non-GM QA user, reload/join as that user, confirm the Territory Scene opens and fits to canvas, then as GM verify the interface is resizable/scrollable, Background changes scene image/color while the grid overlay remains foregrounded, dragging a Character actor imports territory points, and the regenerated Legend label does not overlap the `1` labels. |
| Mortal-Divine tracker startup and resize behavior needs live proof. | Source and validator coverage now enforce auto-open settings, GM tracked roster management, player-owned filtering, scroll/resize CSS, container-width compact layout, and internally scrolling GM roster behavior for #169/#170. Live browser proof has not yet confirmed the startup windows, GM drag/drop/dropdown roster behavior, player-only view, or manual resizing. | Rejoin as GM and non-GM users, confirm the tracker auto-opens without duplicate windows, drag/drop and dropdown-add Character actors as GM, verify players see only owned character balance bars, and resize both views at small/large window sizes. |
| Item sheet spacing and collapse behavior needs live visual proof. | Source and validator coverage now enforce readable field geometry and collapsible rich text sections for shared Item sheets and legacy fallback templates, but the current slice has not visually opened every major Item type in Foundry. | Open representative Occupation, Domain, Power, Bond, Truth, Relic, Worshipper, Vassal, Condition, Gear Quality, Weapon, and Armor sheets at small and normal sizes; confirm no label/input text is clipped and rich-text sections can collapse. |
| Actor and Item edit locks need live permission proof. | Source and unit coverage now enforce permission-aware lock context, locked default state, unlock toggling, disabled controls while locked, and lock controls on Character, Antagonist, Pantheon, and Item sheets. Live Foundry has not yet confirmed the visual lock strip or owner/non-owner behavior across sheets. | As GM/owner/non-owner, open Character, Antagonist, Pantheon, and representative Item sheets; confirm sheets open locked, fields/actions are disabled while locked, unlock enables only permitted edits, tabs/scrolling still work, and non-owners cannot unlock into edit rights. |
| Character ability rows need live visual proof with long text. | Source and unit coverage now enforce compact ability action groups and sanitized player-facing detail prose, but the Borrowed Lives-style screenshot case has not been reopened visually in Foundry after the source fix. | Open `QA Character` or another character with long Blessing/Curse text, expand details, and confirm Add/Details/Hide/Edit controls align cleanly while player-facing text renders as readable prose rather than raw HTML. |
| Existing-world migration has automated coverage but no live snapshot. | Migration behavior is covered for legacy flag preservation and owned Item conversion, but it is not proven against an older or populated Foundry world. | Run v14 with an existing PTG world snapshot and confirm embedded item migration behavior and resulting actor data. |

## Production Blockers / Defects

One product defect was observed and fixed during this pass: active `Combat Controls` could fail in Foundry v14 because `combat.combatants` is collection-backed and was treated as an array in `module/combat/ptg-combat.mjs`. The fix normalizes combatants before dialog rendering, initiative iteration, and round reset updates; the live retest posted the Round and Turn Sequence card. No separate blocker issue was filed because the defect was fixed in this pass.

A second UI defect was observed and fixed during the desktop continuation: Character Creator popout dialogs could inherit low-contrast or non-opaque Foundry window styling, making the creator workflow hard to read. The main creator, validation, and apply-confirm dialogs now use the shared readable sheet-dialog shell and explicit paper backgrounds.

The maps compendium pack has stale embedded Drawing rows in the local checkout while Foundry is open. Source generation, release validation, compendium refresh logic, and Legend label geometry have been fixed, but the shipped `packs/maps` LevelDB files cannot be rebuilt until the running Foundry processes release the pack lock.

The 2026-07-08 desktop continuation confirmed chat-command aliases are not part of the desired workflow, and `PTG GM Setup -> Combat Controls` opens the supported dialog. The same continuation could not complete actor/token or item/sidebar drag/drop through desktop automation; direct automated coverage now exercises the relevant Combat Controls, drop-data, and character-sheet drop handlers, but the live UI gates remain unresolved. Issue #131 cannot close until the unresolved coverage gaps above are completed or explicitly deferred.

## Release Gate

Do not tag a production release from this QA pass yet. Automated checks are green and many runtime paths pass, but #131 still has unverified install, drag/drop, equipped armor, active-combat live, permission live, and migration live-snapshot coverage.
