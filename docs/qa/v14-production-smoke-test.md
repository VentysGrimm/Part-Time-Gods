# Foundry VTT v14 Production Smoke Test

Status: Runtime QA in progress. Issue #131 remains open.

Updated: 2026-07-16
System: Part-Time Gods 2E 0.1.0
Repository: VentysGrimm/Part-Time-Gods
OS: Windows, user data under `C:\Users\Owner\AppData\Local\FoundryVTT`
Foundry target: v14 only

## Environment

| Field | Result |
| --- | --- |
| Foundry version | Pass. Status API reports Foundry `14.364`. |
| Browser | Pass. Codex in-app browser verified setup/join pages; live desktop Foundry window was used for the GM runtime continuation and the 2026-07-14/2026-07-15 Computer Use smoke passes. On 2026-07-16, normal Edge and Edge InPrivate were also used as separate Player3/Player2 clients for the #182 owner/non-owner proof. |
| QA world | Pass. Primary smoke world is `ptg-v14-qa-smoke-test`, system `part-time-gods`, system version `0.1.0`; migrated-world proof also used cloned snapshot `ptg-migration-snapshot-qa` from older local world `tester` (`systemVersion: 0.0.2`). |
| System install from local checkout | Pass. The running QA world is using this checkout at `C:\Users\Owner\AppData\Local\FoundryVTT\Data\systems\part-time-gods`. |
| System install from public manifest/latest release | Pending 0.1.0 release publication and Foundry installer UI test. The live release manifest is reachable but still advertises 0.0.2 on 2026-07-16; the 0.1.0 ZIP asset still returns 404. |
| Screenshots | Not captured as files in this pass. Computer Use visual checks and chat-card evidence are recorded below. |

## Preflight Evidence

| Check | Result | Notes |
| --- | --- | --- |
| JavaScript syntax check | Pass | 2026-07-14: syntax sweep checked 72 `.js`/`.mjs` files with 0 failures. Direct script-runner paths can hit sandbox `EPERM` on `C:\Users\Owner\AppData\Local\FoundryVTT\Data`; the sweep was rerun outside the sandbox. |
| Release validation | Pass | 2026-07-16 latest pass: `npm.cmd run validate` reported 62 actors, 600 premade items, 40 choices, 9 journals, 91 roll tables, 1 scene, 8 macros, and stable source keys. |
| Premade Item source audit | Pass | 2026-07-14: `node scripts\audit-premade-items.mjs` passed outside the sandbox after the expected parent-path `EPERM`; 640 created Item docs, 0 issue buckets. |
| Automated tests | Pass | 2026-07-16: full rules test suite passed outside the sandbox after the expected Foundry data-path `EPERM` in sandboxed execution: 95 tests, 95 pass, 0 fail. Focused #179 creator recovery, dice, source-data, Roll API, #177 observer/non-owner sheet permission, and later #182 balance-bar tests also passed before the full sweep. Follow-up Roll API regression coverage now proves PTG dice use plain awaited `Roll#evaluate()` without `evaluateSync()` or the removed `{ async: true }` option; focused dice tests pass 6/6, combat controls pass 8/8, and sheet edit-lock tests pass 7/7. |
| Release ZIP build | Pass local build | 2026-07-16 Foundry was closed cleanly, `npm.cmd run check:release-assets` reported all declared package pack locks released, and `npm.cmd run zip` created `dist\part-time-gods-0.1.0.zip` with 104 files plus `dist\system.json`. The follow-up release check reports no stale local ZIPs and local ZIP size 5,812,289 bytes. |
| GitHub Actions | Pass | Main branch validation for the drop-data and initiative coverage slice passed at `91094ebd90f293cb4f516e323f3bb3a27aee3152`: `https://github.com/VentysGrimm/Part-Time-Gods/actions/runs/28949168516`. |
| Public manifest install path | Pending 0.1.0 release asset update | `https://github.com/VentysGrimm/Part-Time-Gods/releases/latest/download/system.json` returned HTTP 200 on 2026-07-16, but the decoded live asset still advertises version `0.0.2` and download `v0.0.2/part-time-gods-0.0.2.zip`. |
| Public ZIP install path | Pending for 0.1.0 release asset | `https://github.com/VentysGrimm/Part-Time-Gods/releases/download/v0.1.0/part-time-gods-0.1.0.zip` returned HTTP 404 on 2026-07-16 and must be published before this check can pass for the new version. |
| Physical drag/drop evidence matrix | Pending physical pass | `docs/qa/manual-dragdrop-matrix.md` lists 15 required physical drag rows for #131/#165. 2026-07-16 `npm.cmd run check:dragdrop-evidence` reports 0/15 passed, with Armor Partial from the runtime `DataTransfer` proof, `item-condition` Pending with the failed `Afraid` Computer Use drag evidence, and every other physical row Pending. |

## Required Runtime Flow

| Test | Status | Result / Notes |
| --- | --- | --- |
| Install system from local checkout | Pass | Running Foundry v14.364 world is using this checkout. |
| Install system from release ZIP or manifest | Pending release publication and runtime install test | The live latest-release manifest currently advertises `0.0.2`, and the `0.1.0` ZIP asset returns HTTP 404 as of 2026-07-16. Foundry's install-by-manifest UI still needs a separate clean install target after the 0.1.0 release assets are published. |
| Create a fresh world using the system | Pass | Active QA world is `ptg-v14-qa-smoke-test`. |
| Create Character actor | Pass | `QA Character` exists and was used for sheet, creator, roll, damage, condition, and recovery tests. |
| Create Antagonist actor | Pass | `QA Antagonist` exists. The 2026-07-15 clean F5 Computer Use retest showed the Combat Actions panel visible after the `ptg-panel-section` fix, and Roll Attack posted `QA Antagonist: Attack` with `Successes: 0` and no visible error. |
| Create Pantheon actor | Pass | `QA Pantheon` exists. The 2026-07-15 clean F5 Computer Use retest showed Pantheon Table Tools and Members controls visible/readable after moving non-rich-text panels to `ptg-panel-section`. |
| Populate compendia | Pass | 2026-07-14 Computer Use pass reported `Updated 131 Part-Time Gods compendium entries`; the live Premade Items compendium no longer exposed retired Battle folders and `06. Divine Battles` exposed Battle of Fists/Wits journal pages after the pack rebuild. |
| Create/open territory scene | Pass | GM setup created/opened `God Territory Grid`; Territory Grid app showed 10x10 grid, 0 points, and 0 influenced cells. 2026-07-14 Computer Use proof confirmed the GM interface can scroll, resize without crushing labels, open the readable Background dialog, and use `View` to frame the Territory scene with the overlay fit to the canvas. The post-fix live check repaired stale Legend geometry to `x=8,y=8,w=84,h=42`, kept all 24 managed overlay drawings foregrounded at sort `5000..5023`, applied background image `icons/weapons/swords/sword-broad-serrated-blue.webp` plus color `#112233` through v14 `levels[0].background`, restored the previous background, and captured 0 PTG console errors/warnings. |
| Use character creator with manual choices | Pass | Character Creator completed with valid budgets: Skill Points 10/10, Manifestation Points 4/4, Attachment Points 5/5, Spark 1, Fragments 3, Starting Truth `QA Truth`. Creator, validation, and apply-preview popouts now use readable sheet-dialog styling with opaque paper backgrounds. 2026-07-14 Computer Use proof opened the creator from `QA Character` and traversed all six steps through Final Touches without clipped step text. Follow-up #179 source/test work preserves current selections when validation review reopens the creator, highlights problem steps, reports stale/missing source Items and failed source applications, and fails fast before actor mutation for missing source Items. 2026-07-15 Player4 browser QA completed source-backed creator runs as a true non-GM player; after hard reload, `QA Player4 Promptless Scoped` applied Medical -> Therapist Community, The Wanderer, Elemental, and Cult of the Saints with no follow-up attachment/Dominion prompts, and the sheet showed the expected identity, source choices, source-granted items, starting Bond, and starting Truth. |
| Use Random God helper | Pass for core creator apply path | Random God helper populated source-backed selections including Occupation, Archetype, Dominion, Theology, Blessing, and Curse before final apply. Automated coverage verifies editable divine identity fields, missing-choice fallbacks, selected Occupation/Archetype/Dominion/Theology theme influence, roll-table path logging, the #174 multi-combination matrix for two Archetype/Dominion pairings plus no-choice and one-choice cases, and creator apply persistence for final sheet fields, budgets, starting attachments, starting Truth, and selected source Items. 2026-07-14 Computer Use proof generated Random God suggestion `Aurel of the Borrowed Crown, God/dess of Light`, applied it into editable identity fields, confirmed the creator apply dialog with a valid 10 Skill / 4 Manifestation / 5 Attachment plan, verified Page 1 persisted `God/dess of Light`, and verified Page 2 created level-5 owned Bond `QA Creator Live Bond`. |
| Drag/drop Occupation item | Pending live proof | Drag/drop matrix not yet run live. Automated coverage now verifies shared Item drop-data resolution for UUID, compendium pack, world Item, and embedded data sources, plus character sheet `_onDrop` copying the full #131 item matrix into actor inventory. |
| Drag/drop Archetype item | Pending live proof | Drag/drop matrix not yet run live. Automated coverage now verifies shared Item drop-data resolution for UUID, compendium pack, world Item, and embedded data sources, plus character sheet `_onDrop` copying the full #131 item matrix into actor inventory. |
| Drag/drop Dominion item | Pending live proof | Drag/drop matrix not yet run live. Automated coverage now verifies shared Item drop-data resolution for UUID, compendium pack, world Item, and embedded data sources, plus character sheet `_onDrop` copying the full #131 item matrix into actor inventory. |
| Drag/drop Theology item | Pending live proof | Drag/drop matrix not yet run live. Automated coverage now verifies shared Item drop-data resolution for UUID, compendium pack, world Item, and embedded data sources, plus character sheet `_onDrop` copying the full #131 item matrix into actor inventory. |
| Drag/drop Blessing item | Pending live proof | Drag/drop matrix not yet run live. Automated coverage now verifies shared Item drop-data resolution for UUID, compendium pack, world Item, and embedded data sources, plus character sheet `_onDrop` copying the full #131 item matrix into actor inventory. |
| Drag/drop Curse item | Pending live proof | Drag/drop matrix not yet run live. Automated coverage now verifies shared Item drop-data resolution for UUID, compendium pack, world Item, and embedded data sources, plus character sheet `_onDrop` copying the full #131 item matrix into actor inventory. |
| Drag/drop Truth item | Pending live proof | Drag/drop matrix not yet run live. Automated coverage now verifies shared Item drop-data resolution for UUID, compendium pack, world Item, and embedded data sources, plus character sheet `_onDrop` copying the full #131 item matrix into actor inventory. |
| Drag/drop Relic item | Pending live proof | Drag/drop matrix not yet run live. Automated coverage now verifies shared Item drop-data resolution for UUID, compendium pack, world Item, and embedded data sources, plus character sheet `_onDrop` copying the full #131 item matrix into actor inventory. |
| Drag/drop Bond item | Pending live proof | Drag/drop matrix not yet run live. Automated coverage now verifies shared Item drop-data resolution for UUID, compendium pack, world Item, and embedded data sources, plus character sheet `_onDrop` copying the full #131 item matrix into actor inventory. |
| Drag/drop Worshipper item | Pending live proof | Drag/drop matrix not yet run live. Automated coverage now verifies shared Item drop-data resolution for UUID, compendium pack, world Item, and embedded data sources, plus character sheet `_onDrop` copying the full #131 item matrix into actor inventory. |
| Drag/drop Vassal item | Pending live proof | Drag/drop matrix not yet run live. Automated coverage now verifies shared Item drop-data resolution for UUID, compendium pack, world Item, and embedded data sources, plus character sheet `_onDrop` copying the full #131 item matrix into actor inventory. |
| Drag/drop Condition item | Pending live proof | Drag/drop matrix not yet run live. Automated coverage now verifies shared Item drop-data resolution for UUID, compendium pack, world Item, and embedded data sources, plus character sheet `_onDrop` copying the full #131 item matrix into actor inventory. |
| Drag/drop Weapon item | Pending live proof | Drag/drop matrix not yet run live. Automated coverage now verifies shared Item drop-data resolution for UUID, compendium pack, world Item, and embedded data sources, plus character sheet `_onDrop` copying the full #131 item matrix into actor inventory. |
| Drag/drop Armor item | Partial live proof | 2026-07-14 Computer Use runtime proof dispatched a Foundry-style `DataTransfer` Item payload for world Armor `QA Runtime Armor Drop` onto unlocked `QA Character`; the character gained 1 owned Armor item. This proves the live actor-sheet Item drop handler for Armor payloads, but it is not hardware-level sidebar mouse-drag proof. Automated coverage verifies shared Item drop-data resolution for UUID, compendium pack, world Item, embedded data, HTML `data-uuid`, and `_id` sources, plus character sheet `_onDrop` copying the full #131 item matrix into actor inventory. |
| Roll Skill Combo | Pass | Standard zero-pool Skill Combo posted a Fate Die card. Opposed and extended checks also posted chat cards. 2026-07-14 Computer Use proof opened `Battle of Fists`, scrolled the Skill Combo Check dialog to lower modifiers while keeping the footer visible, and rolled a fresh `QA Character: Battle of Fists` chat card: Base Pool 4d10, Final Pool 4d10, Successes 2, Difficulty 1, Margin 1, Success. |
| Roll Manifestation | Pass | `QA Character: Aegis + Discipline` posted Base Pool 5d10, Successes 3, Difficulty 1, Margin 2, Success. |
| Roll opposed check | Pass | `QA Character: Athletics + Athletics`, Mode Opposed, Base Pool 6d10, Successes 1 vs Difficulty 2, Failure. |
| Roll extended check | Pass | `QA Character: Athletics + Athletics`, Mode Extended, Base Pool 6d10, Successes 3, Extended Progress 5/6, In Progress, Success. |
| Apply Health damage | Pass | Chat card posted `Health Damage`, Raw Damage 2, Applied 2, `Health: 8 -> 6`. |
| Apply Psyche damage | Pass | Chat card posted `Psyche Damage`, Raw Damage 2, Applied 2, `Psyche: 10 -> 8`. |
| Apply armor | Pass | 2026-07-14 Computer Use runtime proof created/updated world Armor `QA Runtime Armor Drop`, set Rating 2, Equipped true, and Fire Proof 2, then dispatched the live Item drop payload onto `QA Character`. The follow-up Health damage proof posted `Health Damage` with reason `QA runtime armor drop proof`, tag `fire`, Raw Damage 6, Armor -2, Tag Armor -2, Applied 2, and `Health: 7 -> 5`; the live readback showed 1 owned Armor copy, equipped true, rating 2, Health 5, and derived Armor 2. |
| Apply Conditions | Pass | Chat card added custom condition `QA Smoke Strain`, category physical, severity 1, to `QA Character`. |
| Recover/reduce Conditions | Pass | `Reduce` removed `QA Smoke Strain`. Explicit `Recover` on `QA Recover Check` rolled Medicine + Empathy and posted `Severity: 1 -> 0`, Outcome Removed. |
| Apply healing | Pass | Character sheet resource restore increased Health from 6/8 to 7/8. Combat-control healing has automated coverage for selected combatant actors with object resources and scalar statblock resources; the #171 regression test verifies `QA Antagonist`-style scalar Health can recover from `0 -> 1` instead of being capped at the current value. 2026-07-14 Computer Use proof used actual actor combatants in the active encounter and posted Combat Controls recovery cards for `QA Character`: Health `6 -> 7` and Psyche `7 -> 8`. |
| Open Combat Controls from GM panel or scene controls | Pass | GM setup panel `Combat Controls` correctly warned before an encounter existed. In the active encounter retest, Combat Controls initially failed because v14 collection-backed `combat.combatants` was treated as an array; this was fixed in `module/combat/ptg-combat.mjs`, Foundry was reloaded, and Combat Controls opened and posted a `Round and Turn Sequence` card. 2026-07-08 desktop continuation verified the `PTG GM Setup` scene control opens `PTG Combat Controls`; chat-command aliases are not part of the desired workflow. Automated coverage verifies collection-backed combatants render in the dialog, `rollPTGInitiative()` updates collection-backed combatants, selected-combatant action markers are recorded, combat-control healing updates actor resources and chat cards, scalar statblock Health can recover above zero, selected Psyche damage updates the actor resource and chat card, and the native Combat Tracker initiative roll path uses the same PTG initiative formula. 2026-07-12 browser QA verified the active Combat Tracker contained `QA Antagonist` and `QA Character`, `PTG GM Setup -> Combat Controls -> Roll PTG Initiative` updated native initiative values from `8/6` to `12/5`, posted the source-backed initiative card, and `Start Combat` advanced the tracker to `Round 1` with `QA Character` before `QA Antagonist`. 2026-07-14 Computer Use proof created a native Combat Tracker encounter, added `QA Character` and `QA Antagonist` from tokens, rolled native initiative, and showed tracker values `QA Antagonist` 10 and `QA Character` 6. The follow-up Computer Use pass reloaded Foundry, opened `PTG GM Setup -> Combat Controls`, confirmed the dialog footer/Post button stayed visible in a scrollable 700x680 shell, and posted live `QA Character` cards for Quick Action, Health damage `7 -> 6`, Psyche damage `8 -> 7`, Health healing `6 -> 7`, and Psyche healing `7 -> 8`. |
| Open Territory Grid from GM panel or scene controls | Pass | GM setup and scene control paths opened Territory Grid tooling and the God Territory Grid scene. 2026-07-14 Computer Use proof confirmed internal grid scrolling, window resizing, readable Background dialog, and `View` canvas framing while preserving the foreground grid overlay. The GM Import control converted `QA Character` into 4 territory points and 2 influenced cells; after a narrow-cell text defect was found, the grid was hard-reloaded and rechecked with point names rendered as horizontal ellipsized chips while the side list retained full details. A separate headless Edge client logged in as role-1 `Player3`; non-GM `game.partTimeGods.openTerritoryInterface()` returned `God Territory Grid` scene `Scene.vZ5MWDgsubN90MZW`, did not render the GM Territory app, and fit the scene to a `756x454` board at scale `0.325`. The 2026-07-15 true F5 reload again auto-opened the integrated GM Territory window. |
| Open Mortal-Divine Balance from GM panel | Pass | GM setup opened Mortal-Divine Balance tracker with `QA Character - Balanced (0)`. 2026-07-12 browser QA reopened the tracker through the scene-control `PTG GM Setup` path, selected `QA Character` from the dropdown, used `Add Character` to populate the GM roster, and confirmed the resizable tracker window stayed contained. 2026-07-14 Computer Use proof confirmed the tracker auto-opened, showed `QA Character - Balanced (0)` plus `Test`, and retained internal scroll/resize behavior down to a compact layout. The follow-up runtime drop proof dispatched a Foundry-style `DataTransfer` Actor payload in the live Electron client and the tracker switched from `QA Character` to `Test`, proving the GM drop handler executes in Foundry. The headless `Player3` proof rendered the player Balance tracker with `Your Mortal-Divine balance`, `Test`, no `QA Character`, and no Add Character, Remove, or Custom Adjustment controls. The compact `Player3` proof found the separate `ptg-balance-tracker` module overwriting `game.partTimeGods.openMortalDivineBalanceTracker` with a GM-only legacy opener; after the system reasserted its integrated API, `game.partTimeGods` and `game.ptg.balance` used the same player-safe function, returned the player app, rendered a marked player view at `420x360`, and produced 0 console errors/warnings. The 2026-07-15 true F5 reload again auto-opened the integrated Mortal-Divine tracker. On 2026-07-16, Electron GM clicked `Show Player Bar` with normal Edge Player3 and Edge InPrivate Player2 connected; Player3 received the compact `GM-shared character balance` bar for `Character - Balanced (-1)`, and Player2 received no Mortal-Divine window or GM controls. |
| Open Opposition Builder from GM panel | Pass | GM setup opened PTG Opposition Builder and created `QA Builder Antagonist`; the created antagonist sheet rendered with builder notes. |
| Test GM permission boundary | Pass | 2026-07-11 browser continuation rejoined the active world as `Player2` at `/game` and verified the visible non-GM UI does not expose `PTG GM Setup`, `Combat Controls`, `Territory Controls`, `Opposition Builder`, or `Mortal-Divine Balance` controls. Automated coverage verifies non-GM users cannot open Combat Controls. Later Player3/Player4 role proofs did not expose GM-only controls to non-GM clients. |
| Test owner permission boundary | Pass | The active QA world has role-1 `Player3` assigned to owned character `Test`; a headless Edge client logged in as `Player3` successfully and saw the player-facing Territory scene plus only `Test` in the Balance tracker. The follow-up in-app browser proof verified Player3-owned `Test` defaulted editable, could be locked, kept allowed sheet controls usable, and did not require chat commands. Automated coverage verifies non-owner, non-GM users cannot apply Health damage to an actor. |
| Test observer permission boundary | Pass | 2026-07-15 Player4 observer proof set `QA Character` to OBSERVER for true non-GM Player4 from the GM ownership dialog, reloaded the Player4 browser client, verified `QA Character` appeared in the Player4 Actors directory, opened the sheet with the `Read Only` banner, found 0 editable textareas and 4 readonly textareas, confirmed mutation/play-roll controls stayed disabled, switched to Page 2, expanded an item Details row, and confirmed detail bodies remained internally scrollable. |
| Test non-owner permission boundary | Pass for damage denial and actor visibility | 2026-07-15 Browser proof joined as true non-GM `Player4`, reached a rendered `/game` UI, showed only `Player4` and `Gamemaster [GM]` in the player list, exposed no Actor entries in the Actors directory, clicked a visible `QA Character` Health Damage chat-card action, posted no new chat message, and displayed the expected warning `No editable actors are available for applying damage.` |
| Test migration against existing world | Pass | Automated coverage verifies legacy attachment text is preserved in actor flags and legacy sheet notes are converted into owned Items without duplicating already migrated fields. 2026-07-15 Computer Use cloned older local world `tester` (`systemVersion: 0.0.2`) to `ptg-migration-snapshot-qa`, fixed an initial cloned-manifest BOM parse error, accepted Foundry's `0.0.2 -> 0.1.0` migration with backup, joined as GM, and live-reloaded after the locked-compendium startup fix with no red locked-pack warning. |

## Runtime Evidence Notes

- `QA Character` creator apply path persisted Occupation `Medical - Therapist`, Archetype `The Wanderer`, Dominion `QA God of Smoke Tests`, Theology `Cult of the Saints`, Health 8, Psyche 10, Free Time 4, Wealth 5, and Fragments 3 before later damage tests.
- Character Creator popouts were tightened after the desktop continuation exposed poor readability: the main creator, validation, and apply-confirm dialogs now share the readable `ptg-sheet-dialog` shell and explicit paper backgrounds.
- 2026-07-08 desktop continuation reloaded Foundry and visually verified the `QA Character` Creator popup and Character Creator Validation popup render with opaque paper backgrounds, readable dark text, and clear purple action buttons.
- 2026-07-11 source-side readability continuation: the in-app browser reopened `QA Character` at a Foundry-safe viewport and confirmed the printed title rows keep `Name:` and `God/dess Of:` labels plus their input boxes inside 44px-high containers with white input backgrounds and black text. A direct browser click opened the `QA Character: Add Condition` dialog and exposed a remaining plain Foundry `application dialog` case where PTG CSS variables were not inherited by select/input controls; stylesheet fallbacks now give PTG dialog fields concrete white backgrounds, black text, bounded widths, and internal overflow handling even when the outer dialog lacks the `part-time-gods` class.
- Manifestation Measures posted `Aegis Measures` with 3 successes available, 1 measure spent on Damage, 2 unspent successes, and QA notes.
- Condition recovery posted both a recovery roll card and a condition recovery summary card.
- Opposition Builder generated a real world Actor instead of only opening the dialog.
- Active-combat retest created and started Combat encounter 1, then verified the GM panel `Combat Controls` dialog opened after the v14 combatants collection fix and posted `Round and Turn Sequence` to chat.
- Shared drop-data resolver coverage verifies Item drops from UUID, compendium pack, world Item, embedded data, HTML `data-uuid`, and `_id` payloads, which supports the actor-sheet drag/drop matrix but does not replace the required physical sidebar-to-sheet drag pass.
- Character-sheet drop coverage verifies `_onDrop` copies the full #131 item matrix into actor inventory, applies core choice drops through `actor.applyChoice()`, and rejects an Armor item dropped into a typed Truth section.
- Combat-control coverage verifies dialog option rendering, initiative updates, selected-combatant action markers, selected Psyche damage, object-resource healing, scalar statblock Health recovery from `0 -> 1`, and collection-backed actor Item handling for weapon/armor helper paths. 2026-07-12 live browser QA proved the source-backed initiative workflow in the active Foundry Combat Tracker: Combat Controls rerolled `QA Antagonist` and `QA Character`, updated their native initiative fields to `12` and `5`, posted the PTG initiative procedure chat card, and started `Round 1` in native turn order. 2026-07-14 Computer Use proof also used Foundry's native Combat Tracker path directly: `QA Character` and `QA Antagonist` were added from canvas tokens, native initiative rolls populated the tracker, and the live order showed `QA Antagonist` 10 above `QA Character` 6. The same date's follow-up Electron pass proved the GM panel Combat Controls action/damage/healing path against those actor combatants with expected chat-card deltas for Quick Action, Health, and Psyche.
- Permission-boundary coverage now verifies non-GM Combat Controls denial and non-owner damage denial in automated tests. Owner, observer, and non-owner role behavior now have live Foundry proof: Player3 owner edit-lock behavior, Player4 non-owner damage denial, and Player4 OBSERVER read-only sheet rendering on `QA Character`.
- Migration coverage verifies both existing-world safety paths: schema migration preserves legacy attachment text in actor flags, and canonical embedded Item migration converts legacy condition/relic sheet notes into owned Items while skipping a duplicate already migrated bond field.
- 2026-07-08 desktop continuation: Foundry v14.364 reported active world `ptg-v14-qa-smoke-test`, system `part-time-gods` 0.0.2, and one connected user. GM Setup opened from the Part-Time Gods scene controls, and `Combat Controls` opened the `PTG Combat Controls` dialog. GM panel controls are the supported runtime path for this pass; chat-command aliases are intentionally out of scope.
- 2026-07-11 version alignment: local Foundry status API reported active world `ptg-v14-qa-smoke-test`, system `part-time-gods`, system version `0.1.0`; package, manifest, validation, and release ZIP references were aligned to `0.1.0`. A follow-up live release check found the latest GitHub Release `system.json` asset is still `0.0.2` and the advertised `v0.1.0/part-time-gods-0.1.0.zip` asset returns HTTP 404, so the public install path remains pending release asset publication.
- 2026-07-12 status refresh: `http://127.0.0.1:30000/api/status` returned Foundry `14.364`, active world `ptg-v14-qa-smoke-test`, system `part-time-gods`, system version `0.1.0`, and 1 connected user. The local checkout still has live LevelDB pack locks, so source-side validation can continue but pack rebuild/release ZIP work should wait until Foundry is closed.
- 2026-07-15 Computer Use live GM continuation: Foundry setup Package Warnings (6) were identified as non-PTG installed-system warnings: `blades-in-the-dark`, `lone-wolf-fists` (`template.json` plus unknown `gridDistance`/`gridUnits`, counting as two warnings), `mutants-and-masterminds-3e`, `vaesen`, and `wfrp4e`. No Part-Time Gods package warning appeared. The PTG QA world opened as `Gamemaster` on `God Territory` with no blocking error modal. `QA Character` opened locked with title fields contained, the Creator opened from the locked sheet, steps 1-6 navigated, and the long final step scrolled internally. The native Combat Tracker showed Round 1 with `QA Antagonist` initiative 10 and `QA Character` initiative 6. The Items sidebar loaded QA all-sheet sweep entries; representative Archetype item sheet content stayed in its scrollable sheet body and collapsible Description panel. The Rules Reference compendium expanded and rendered `06. Divine Battles` with Battle of Fists/Wits/Gear Quality pages as journal rules content. `C:\Users\Owner\AppData\Local\FoundryVTT\Logs\error.2026-07-15.log` was 0 bytes at the time of the pass.
- 2026-07-15 Computer Use setup/package sweep and #180/#181 compendium recheck: after returning the live Electron client to Setup, the Package Warnings panel still reported 6 warnings, all from other installed game systems: `blades-in-the-dark`, `lone-wolf-fists` twice (`template.json` and unknown `gridDistance`/`gridUnits`), `mutants-and-masterminds-3e`, `vaesen`, and `wfrp4e`. No Part-Time Gods warning appeared. In the QA world, the live `Part-Time Gods Premade Items` pack returned no entries for `Manifestation Applications`, `Critical Failure`, or `Gear Qualities`; the live `Part-Time Gods Rules Reference` rendered `04. Divine Expressions -> Manifestation Applications: Aegis, Beckon, Journey, Minion, and Oracle`, `05. Dice, Skills, and Resources -> Critical Failure Effects`, and `06. Divine Battles -> Gear Qualities: Armor and General` / `Gear Qualities: Weapon` as journal pages. `error.2026-07-15.log` remained 0 bytes after the pass and `npm.cmd run validate` passed with 62 actors, 600 items, 40 choices, 9 journals, 91 roll tables, 1 scene, 8 macros, and stable source keys.
- Issues #180 and #181 were closed as completed on 2026-07-15 after posting the copied-pack audit, live Electron compendium searches, Rules Reference journal rendering proof, no-PTG-package-warning proof, empty runtime error log, and passing release validation evidence to GitHub.
- 2026-07-15 Computer Use #179 live creator recovery proof: from Foundry Setup, joined `PTG v14 QA Smoke Test` as `Gamemaster`, created fresh Character actor `QA Creator Retry Live`, opened the Character Creator from the locked sheet, and intentionally applied incomplete choices. The validation dialog listed skill points 0/10, manifestation points 0/4, missing starting Truth, and fragments 0 instead of 3. Clicking `Review` reopened the creator with current state preserved, displayed the `Review Needed` alert, and highlighted Step 6 / Final Touches. The corrected retry set Athletics 10, Aegis 4, starting Truth `QA Creator Retry Truth`, and Starting Fragments 3; the apply preview showed Skill Points 10/10, Manifestation Points 4/4, Attachment Points 5/5, Spark 1, Starting Fragments 3, and the starting Truth. Confirming posted `Updated character creator details`, and Page 2 showed `QA Creator Retry Truth` under Truths. `error.2026-07-15.log` stayed 0 bytes and release validation passed after the pass.
- 2026-07-15 Player4 browser Character Creator QA created/opened player-owned `QA Player4 Creator Live`, verified the Character sheet defaulted unlocked/editable, selected Medical -> Therapist Community, The Wanderer, Elemental, Cult of the Saints, a level-5 Bond, full Skill/Manifestation budgets, Spark 1, Fragments 3, and identity fields, then confirmed the apply preview. The live run exposed hidden duplicate dependent option values and old cached source-choice follow-up prompts; source now parent-scopes Archetype and Occupation career option values, disables hidden dependent options, and supplies non-interactive attachment definitions plus Dominion defaults during creator application. After a hard reload, fresh player-owned `QA Player4 Promptless Scoped` applied the same source-backed path with the visible Medical/Therapist label preserved, posted all four source-choice chat cards, opened no follow-up attachment/Dominion/validation dialogs, and read back the expected identity/source/item/resource values including `Therapist Community`, `QA Promptless Scoped Bond`, and `QA Promptless Scoped Starting Truth`. Visual checks found no broken visible images, title boxes contained Name and God/dess Of, lined textareas used local line backgrounds with internal scrolling, and `error.2026-07-15.log` remained 0 bytes.
- 2026-07-15 source visual follow-up: printed Character sheet title rows now have tighter containment, rich-text/detail images are bounded with centered `object-fit: contain`, and printed line-backed fields/lists clip their local backgrounds to the holding box while preserving internal scrolling. `node --check scripts\validate-release.mjs`, `git diff --check -- styles\part-time-gods.css scripts\validate-release.mjs`, and `npm.cmd run validate` passed.
- 2026-07-15 image/line containment follow-up: fallback-wired sheet/app images, actor portraits, Pantheon member portraits, and Balance tracker portraits now use contained rendering instead of crop rendering; printed ruled textareas/lists have max-height and local scroll guards so ruled lines stay inside their boxes. Release validation now guards the new selectors. `node --check scripts\validate-release.mjs`, `git diff --check -- styles\part-time-gods.css scripts\validate-release.mjs`, `rg -n "object-fit: cover|img\[data-fallback-src\]|ptg-print-ability-list|ptg-print-truth-list|max-height: 22rem|max-height: 18rem" styles\part-time-gods.css scripts\validate-release.mjs`, and `npm.cmd run validate` passed; the grep found no remaining `object-fit: cover` sheet portrait rules.
- 2026-07-15 image/table/line polish follow-up: legacy Domain and Power item templates now use the shared `itemImg` / `itemImageFallback` context; printed title labels stay on one neat line beside scrollable value boxes; rich-text tables, table cells, media, and horizontal rules are bounded so they stay inside their holding boxes. `node --check scripts\validate-release.mjs`, `node --check scripts\check-release-assets.mjs`, `node --check module\sheets\item-sheet.mjs`, `git diff --check -- styles\part-time-gods.css templates\item\domain-sheet.hbs templates\item\power-sheet.hbs scripts\validate-release.mjs`, and `npm.cmd run validate` passed.
- 2026-07-16 image rendering/line follow-up: fallback-wired images, sheet portraits, rich-text/detail images, printed-page media, horizontal rules, older line fields, and ruled textarea/list backgrounds now stay contained in their boxes. The lined surfaces use `padding-box` clipping/origin with paint containment so rules stay neat while the field scrolls. `node --check scripts\validate-release.mjs`, `git diff --check -- styles\part-time-gods.css scripts\validate-release.mjs`, and `npm.cmd run validate` passed.
- 2026-07-16 release packaging update: after Foundry was closed, pack locks released and `npm.cmd run zip` created `dist\part-time-gods-0.1.0.zip` plus `dist\system.json`. The follow-up `npm.cmd run check:release-assets` reports local packaging clean: no stale local ZIPs, local ZIP present at 5,812,289 bytes, local manifest version `0.1.0`, and local download URL correct. The same check still fails remote publication: latest release manifest is HTTP 200 but version `0.0.2`, latest release download still points at `v0.0.2/part-time-gods-0.0.2.zip`, and the `v0.1.0` ZIP asset is HTTP 404. Local browser/CLI release upload is unavailable in this session, so `.github/workflows/release.yml` now publishes the matching assets through GitHub Actions after this state reaches `main` and the `v0.1.0` tag/workflow is run.
- 2026-07-16 manual drag/drop evidence gate follow-up: `docs/qa/manual-dragdrop-matrix.md` now enumerates the required physical item rows for Occupation, Archetype, Dominion/Domain, Theology, Blessing, Curse, Truth, Relic, Bond, Worshipper, Vassal, Condition, Weapon, Armor, plus the #165 Character actor-to-Territory row. `npm.cmd run check:dragdrop-evidence` is intentionally red until the physical pass is complete; the current escalated run reports 0/15 Pass, Armor Partial from the runtime `DataTransfer` proof, `item-condition` Pending with the failed `Afraid` Computer Use drag evidence, `actor-territory-character` Pending, and every other row Pending.
- 2026-07-15 #182 source/live slice: the GM Mortal-Divine tracker now has a tracked-character `Show Player Bar` control that targets non-GM owners without depending on stale active flags, sends a socket message to open a compact player bar with only the owned character context and meter, warns the GM when no owning player is available, and refreshes a shown player bar after later balance adjustments in the same GM session. Release validation guards the socket/API/template/CSS pieces, and `node --test tests\rules\source-data.test.mjs` passed 31/31 outside the sandbox after the expected Foundry data-path `EPERM`. Live browser proof joined as Player3, confirmed the current world grants Player3 OWNER on `Character`, opened a second in-app tab as GM, clicked `Show Player Bar`, and saw `Sent Character's balance bar to Player3.` The same pass found and fixed a delegated-click regression for boolean `data-balance-action` preset buttons. On 2026-07-16, separate Electron GM, normal Edge Player3, and Edge InPrivate Player2 clients proved owner receive and non-owner absence, and #182 was closed as completed after GitHub evidence comment `4993637128`.
- 2026-07-15 #177 final live D&D5e/PTG side-by-side proof: Computer Use opened D&D5e Character `Friar Briar`, verified Play/Edit mode, header/sidebar readouts, grouped embedded Feature controls, expanded `Martial Arts` details, and editable-only controls in Edit mode. The same pass returned to the PTG QA world as `Gamemaster`, confirmed Territory and Mortal-Divine interfaces auto-opened, opened `QA Character`, verified locked/unlocked modes, performed and restored a live name edit, checked contained title/resource/line/image layout, expanded `Borrowed Lives` Details with internal rich-text scrolling, and scrolled Page 2 through Bonds, Worshippers/Vassals, Relics, Truths, and Gear/Notes without box spill or row crush. GitHub evidence comment `4985544256` was posted and #177 was closed as completed.
- 2026-07-15 #131 migrated-world snapshot proof: older local world `tester` (`systemVersion: 0.0.2`) was cloned to `ptg-migration-snapshot-qa`, the cloned `world.json` was re-saved without the initial UTF-8 BOM after Foundry logged a setup parse error, and Foundry then showed the world migration dialog from `0.0.2` to installed system `0.1.0`. Migration was run with Foundry backup enabled, the clone opened as `Gamemaster`, and the first migrated startup exposed red `locked compendium "part-time-gods.premade-items"` notifications from ready-time auto-populate. Ready-time population now calls `populatePremadeCompendiums({ skipLockedPacks: true })`, which skips protected package packs before create/update/delete calls while leaving the manual GM Setup populate action deliberate. Focused source-data tests passed 28/28 outside the sandbox after the expected Foundry data-path `EPERM`, `npm.cmd run validate` passed, and a live F5 reload of the migrated clone showed only the normal blue `Part-Time Gods compendiums are already populated.` notification with no delayed red locked-pack warning.
- Issue #179 was closed as completed on 2026-07-15 after posting the source/test and live invalid-apply/review/corrected-retry evidence to GitHub.
- 2026-07-15 Computer Use clean F5 actor/system smoke: the true client reload auto-opened the integrated Territory and Mortal-Divine windows. After closing those startup windows, `QA Pantheon` showed Table Tools and Members controls visibly instead of collapsed strips, `QA Antagonist` showed Combat Actions visibly, and Antagonist Roll Attack posted `QA Antagonist: Attack` with `Successes: 0`. The layout defect was fixed by moving non-rich-text actor action/member panels to `ptg-panel-section`, adding compact-sheet no-shrink CSS, and adding release-validation guards for the Pantheon and Antagonist templates. `error.2026-07-15.log` stayed 0 bytes, `npm.cmd test` passed 87/87, and `npm.cmd run validate` passed.
- 2026-07-12 browser refresh: the in-app browser joined `/game` as `Player2` without a password, but the visible roster showed `Player2 [GM]`, so this route cannot prove non-GM/player-only behavior. At the default 1280x720 viewport Foundry displayed its minimum-size warning; a temporary 1280x900 viewport removed that warning. The first `/game` load briefly showed the integrated `PTG Territory Grid` window with `GM Controls`, `View`, `Move`, `Background`, `Import`, `Clear`, and `Refresh`, but after reload at the supported viewport the Territory Grid window was not open, so startup/resize proof remains incomplete.
- 2026-07-12 premade Item source audit: `scripts/audit-premade-items.mjs` now audits both `character-creation` and `premade-items` source collections. It reported 720 created Item docs, 40 character-creation Items, 680 premade Items, zero Battle of Fists/Wits action Items, and zero invalid types, invalid folders, duplicate type/name pairs, duplicate source IDs, journal-style Item kinds, `journal:` source IDs, or missing source IDs. Shipped/live pack verification remains pending until the LevelDB pack churn is intentionally reconciled.
- 2026-07-12 rules journal audit: `module/data/complete-rules.json` now contains 9 source-backed rules JournalEntries with 51 pages and 6,672 total summary words. The repeated placeholder sections were replaced with source-page-specific summaries, Chapter One's The Descending Storm setting spine was added as a release-safe journal summary, Battle of Fists/Wits action rules were moved out of premade `power` Items into dedicated `06. Divine Battles` journal pages, Otherworld Travel was moved out of Power Items into the `Rituals and Otherworlds` journal procedure, and release validation now fails boilerplate, extractor artifacts, repeated rules paragraphs, thin pages, missing `safeSummary` flags, battle action Items, or oversized rules-reference text.
- 2026-07-14 stale pack source remediation and live recheck: Computer Use first found live retired `Battle-fistss`/`Battle-witss` folders and repeated `Timing, Initiative, and Turns` pages in `06. Divine Battles`; runtime compendium seeding was updated to recognize old slugged Battle folder labels as retired and explicitly refresh managed `JournalEntryPage` rows for rules-reference journals. After Foundry was closed, the targeted `premade-items` and `rules-reference` packs were rebuilt and direct LevelDB verification showed 18 valid Item folders, no retired Battle folders, no battle action Item docs, 51 rules pages, the expected Battle of Fists/Wits pages, and no duplicate rules page names. The follow-up Computer Use pass reopened Foundry, saw `Updated 131 Part-Time Gods compendium entries`, verified Premade Items contains only the expected Item folders and no `Battle` search results, and opened readable Battle of Fists/Wits journal pages in `Part-Time Gods Rules Reference -> 06. Divine Battles`. Source-data tests cover both regressions, release validation guards the helper tokens and duplicate page names, `npm.cmd run validate` passed, and `npm.cmd test` passed 72/72 outside the sandbox after the expected parent-path `EPERM`.
- 2026-07-14 #172 Otherworld live spot-check: Computer Use opened `Part-Time Gods Rules Reference -> 04. Divine Expressions -> Rituals and Otherworlds` and verified the live journal text states Otherworld travel belongs in the procedure journal rather than standalone power Items. The live `Part-Time Gods Premade Items` search for `Otherworld` returned only legitimate Truth `Otherworldly Sight`, with no stale `Otherworld Travel` Item. A precise source-side check found no forbidden `Otherworld Travel`, `Battle of Fists Actions`, `Battle of Wits Actions`, or `Actions and Defenses` Items, and confirmed the expected rules pages live in `04. Divine Expressions` and `06. Divine Battles`.
- 2026-07-14 #172 representative compendium sweep: `scripts/audit-premade-items.mjs` reported 720 created Items across `character-creation` and `premade-items` with 0 invalid types, invalid folders, duplicate type/name pairs, duplicate source IDs, journal-style Items, `journal:` source IDs, or missing source IDs. The rules source has 9 JournalEntries and 51 pages. Computer Use confirmed the live `Part-Time Gods Character Creation` pack exposes the expected `Archetypes`, `Dominions`, `Occupations`, and `Theologies` folders, and representative searches returned `Medical` under Occupations and `Ascendants` under Theologies. This closes the source/live evidence loop for #172; broader production smoke can still sample individual entries as part of #131.
- Issue #172 was closed as completed on 2026-07-14 after posting the source audit, validation, and live compendium spot-check evidence to GitHub.
- 2026-07-12 Skill Combo dialog source/live slice: the Skill Combo Check dialog now has a dedicated bounded scroll wrapper, a bounded flex chain, `standard-form` coverage for Foundry's DialogV2 wrapper, and a content-based `:has(.ptg-skill-combo-options)` fallback so the wrapper/options body scroll internally while the footer remains usable. Release validation and the full Node harness passed after the expected sandbox `EPERM` rerun outside the sandbox. Live Foundry proof at 1280x720 opened `QA Character`, scrolled to Athletics, opened `Skill Combo Check`, and confirmed the 997px options body was constrained to a 478px scroll area; scrolling moved the options panel from `scrollTop 0` to `519`, revealed the pool preview inside that panel, and kept the Roll footer visible. Follow-up live proof at 1280x900 after the wrapper-scroll patch confirmed the scroll wrapper uses `overflow-y: auto`, the options body remains constrained at 560px for 997px of content, browser scrolling moved the options panel from `scrollTop 0` to `437`, and the Roll footer stayed visible.
- 2026-07-12 Character sheet scroll source/live slice: Character sheets now capture active tab scroll before click, submit, item action, and resource button mutations, including programmatic item/resource handlers that can re-render the sheet, then run a post-render restore pass for non-anchor actions after browser/focus layout settles. Unit coverage verifies the actionable-control selector used by the capture hook, release validation passed, and the full Node harness passed after the expected sandbox `EPERM` rerun outside the sandbox: 59 tests, 59 pass. Live Foundry proof at a 1280x900 viewport opened `QA Character`, confirmed Page 1 restored from Page 2 back to `scrollTop 900`, expanded `QA Truth` details on Page 2 with the row still visible (`477 -> 460`), used the same item with the row still visible (`460 -> 438`), unlocked/relocked the sheet without moving the active panel (`484 -> 484`), changed Fragments down/up while the controls were actually visible and restored the actor to 3 Fragments (`484 -> 484`), and opened a Skill Combo dialog from a coordinate-clicked visible Athletics button without moving Page 1 (`763 -> 763`).
- 2026-07-11 Territory integration source slice: Territory now registers an integrated world-ready auto-open setting, scene control visibility for GMs or worlds with a Territory scene, a resizable/scrollable GM interface, GM-only edit/create/import/background/clear paths, Character actor drop import for territory attachments, player routing to the Territory Scene, and fit-to-canvas scene viewing. The point model stores rulebook-facing location type, control source, boundary/status, discovery state, public notes, GM secrets, footprint, Dominion/theology tags, linked Bond/Actor/Item UUIDs, and ritual/event state while player-facing context filters hidden points and GM-only details. Validation passed with `npm.cmd run validate`, source-data Territory tests passed, and the full Node harness passed after the expected sandbox `EPERM` rerun outside the sandbox: 30 tests, 30 pass. Live browser proof joined `/game` after selecting `Player2`; the integrated Territory Grid window auto-opened and showed `God Territory Grid`, `GM Controls`, 3 points, and 2 influenced cells. This proves the earlier world-ready GM/interface startup path, but not the revised player Territory Scene route or GM background/drop controls live.
- 2026-07-12 Territory scene label source slice: the managed `Legend` drawing is inset inside the top-left label cell and uses a smaller text box/font so it no longer shares the full corner cell with the row/column number layout. Source-data tests now assert the Legend box ends before the row-1 and column-1 label cells. Live scene regeneration/visual QA remains pending because `packs/maps` is locked while Foundry is running.
- 2026-07-12 Mortal-Divine tracker source/live GM slice: the tracker now has a world-ready auto-open setting, GM party-style tracked roster persistence, Character actor dropdown/add/remove controls, Character actor drop import, a read-only player view limited to owned character balance bars, explicit resize/scroll CSS for GM and player modes, container-width responsive layout, and an internally scrolling GM party roster. Automated source-data coverage verifies tracked UUID normalization, GM roster visibility, player owned-character filtering, and fallback player-owned display. Live Foundry proof at a temporary 1280x900 viewport opened `PTG GM Setup`, confirmed a single `Mortal-Divine Balance` tracker window, selected `QA Character`, clicked `Add Character`, and showed the selected party card open with Focus/Remove controls. At the normal 820x680 app size, the GM detail pane stayed inside the app frame with `scrollHeight 926`, `clientHeight 520`, and `overflow-y: auto`; scrolling moved it to `scrollTop 406` and brought Custom Adjustment and Recent Movement controls into the visible app frame. After manual resize to about 677x597, the compact stacked layout kept the header selector/Add button and party Focus/Remove controls visible; the tracker body became the internal scroll container with `scrollHeight 1094`, `clientHeight 437`, and `overflow-y: auto`; scrolling to `scrollTop 620` brought Apply and Clear Log controls into the visible app frame. This July 12 player-facing gap was later superseded by the true non-GM Player3 proof and compact player proof recorded below.
- 2026-07-11 Item sheet readability source slice, expanded and live-verified 2026-07-12: shared Item sheet fields now enforce readable input/select/textarea height, padding, and line-height across the active `PTGItemSheet` path and the legacy Domain/Power fallback templates; rich text Item sections are now open-by-default collapsible panels so dense sheets can be compressed instead of clipping labels or field rows. Release validation guards enforce the shared item field geometry, collapsible editor markup, fallback artwork metadata, and long-field scroll classes. Live Foundry proof opened world Armor `QA Armor Live Proof` and Truth `Truth` sheets through the Items sidebar. At 1366x900, Armor used the shared `.ptg-sheet` scroll body (`scrollHeight 1371`, `clientHeight 690`) with 3 long fields, and scrolling moved to `scrollTop 650`; Truth used the same internal sheet scroll body (`scrollHeight 2683`, `clientHeight 690`) with 7 long fields. At the compact 1024x768 viewport, both Item sheets remained inside the viewport (`720x720` app frame), the internal scroll bodies stayed bounded (`clientHeight 650`; Armor max scroll 721, Truth max scroll 2033), and item artwork resolved to the fallback `icons/svg/item-bag.svg` with a loaded natural width instead of a broken image.
- 2026-07-14 #168 all registered Item sheet live sweep: Computer Use created QA-only world Items for every registered Item type in `system.json` and opened each in the live Electron Foundry client at compact `520x520`: Occupation, Archetype, Domain, Theology, Power, Attachment, Bond, Truth, Relic, Worshipper, Vassal, Blessing, Curse, Condition, Gear Quality, Weapon, and Armor. Every sheet measured an internal `486x450` scroll body with `overflow-y: auto`; scroll heights ranged from 1,832 to 4,639px; controls ranged from 8 to 21 per sheet; collapsible editor/details sections ranged from 3 to 8; and the DOM audit reported `badControls: []`, `badLabels: []`, and `badEditors: 0` for all 17 types. All missing/broken QA image paths fell back to `icons/svg/item-bag.svg` with `complete: true` and `naturalWidth: 512`; a short-content valid-art spot check also proved representative Weapon and Occupation images render directly with `naturalWidth: 256`, while a bad Bond path fell back cleanly. A GitHub evidence comment was posted to #168, then the issue was closed as completed.
- 2026-07-11 edit-lock source slice, corrected and live-verified 2026-07-12, then advanced for #177 on 2026-07-14: Character sheets keep the permission-aware edit-lock strip, GM-opened owner sheets keep protected mode for accidental-edit prevention, player-owned sheets default editable, locked sheets block fields/destructive buttons/drop edits, and explicit unlock/lock behavior remains in `module/sheets/sheet-edit-lock.mjs`. The #177 D&D5e comparison confirmed `dnd5e.mjs` keeps locked/read-mode textareas readable by setting `TEXTAREA.readOnly = true` while disabling other controls; PTG now mirrors that functional behavior by making locked/read-only Character textareas `readonly` with `aria-readonly` instead of `disabled`, so long notes remain selectable and internally scrollable without becoming editable. Unit coverage verifies default locked GM-owner state, player-owned editable state, unlock toggling, permission denial, locked-owner gameplay-button exemptions, read-only gameplay denial, and readonly textareas. Release validation guards the #166 character-only boundary and the readonly-textarea lock helper/CSS tokens. Live Foundry proof opened `QA Character` locked with 46 disabled inputs, 46 disabled resource buttons, 25 disabled edit/delete/create controls, 31 enabled roll buttons, and 14 enabled detail buttons; roll, detail, item-use, and scroll actions remained usable while locked during the #178 pass. Live `QA Antagonist` and `QA Pantheon` sheets had no edit-lock toggle, and an owned `Truth: QA Truth` Item sheet opened through Character sheet Edit had no edit-lock toggle and normal editable fields. The Character sheet was relocked after the pass.
- 2026-07-14 #177/#174 sheet creator cleanup: The repeated Common Actions strip was removed from the Character sheet header so play controls are not duplicated above the printed actor pages. Player-owned Character sheets now open editable by default while GM-owned sheets keep the accident-prevention lock. The Character Creator now has backed Step Five attachment rows that create owned Bond, Relic, Worshipper, or Vassal Items and validate their levels against the planned 5 Attachment Points. Follow-up #177 coverage in `sheet-edit-lock.test.mjs` proves observer and non-owner Character sheets remain read-only even after lock toggles, keep details/tabs usable, block roll/use/edit mutation controls, and keep owner-level player sheets editable by default. Focused creator coverage lives in `character-creator.test.mjs`.
- 2026-07-14 #177 true player-owner browser proof: the in-app browser joined the live world through /join as non-GM Player3 with assigned Character Test, set a supported 1280x900 viewport, opened Test from the Actors sidebar, and confirmed the Character sheet defaulted unlocked with Unlocked Editing enabled, data-ptg-sheet-locked=false, 45 enabled inputs, 4 editable textareas, enabled Creator/Edit/Delete controls, and 31 enabled roll buttons. Toggling the sheet lock changed it to Locked Protected from accidental edits, disabled mutation controls including Edit/Delete, made 4 textareas readonly, kept all 31 roll buttons plus 6 detail buttons and Creator usable, then the sheet was restored to the default unlocked state. Browser logs for this pass showed no PTG errors; the only warnings were the already-tracked Easy Target Token/TokenLayer deprecations, plus the initial browser-height warning before the viewport was resized.
- 2026-07-15 #177 non-owner browser proof: the join screen exposed Gamemaster, Player2, Player3, and Player4. After the viewport was raised to 1400x900, Player4 joined as a true non-GM and reached a rendered `/game` UI. The player list showed only `Player4` and `Gamemaster [GM]`; the Actors directory showed no Actor entries for Player4. Clicking a visible `QA Character` Health Damage chat-card action posted no new chat message and displayed the expected warning `No editable actors are available for applying damage.` `error.2026-07-15.log` stayed 0 bytes. The follow-up observer proof below closes the remaining live permission-boundary role slice.
- 2026-07-15 #177 observer browser proof: from the live GM Electron client, `QA Character -> Configure Ownership` was opened and only Player4 was changed from Default to Observer. After reloading the Player4 browser client, the Player4 Actors directory showed `QA Character` and `QA Player4 Creator Live`; opening `QA Character` produced `Character: QA Character` with the `Read Only` banner. The observer sheet had 0 editable textareas, 4 readonly textareas, 147 disabled buttons, disabled Creator/Edit/Delete/Award/Spend/Use/Battle/Aegis mutation/play-roll controls, enabled Page 1/Page 2 tabs and Details controls, and no enabled mutation controls. Page 2 navigation worked, an item Details row expanded, and visible detail bodies reported internal `overflow-y: auto` scrolling with bounded max heights. `error.2026-07-15.log` stayed 0 bytes. GitHub evidence comments were posted to #177 as issue comment `4985098942` and #131 as issue comment `4985099020`.
- 2026-07-15 #177 focused source recheck: `node --test tests\rules\sheet-edit-lock.test.mjs` first hit the known sandbox `EPERM` on `C:\Users\Owner\AppData\Local\FoundryVTT\Data`, then passed outside the sandbox with 7/7 tests.
- 2026-07-15 #177 D&D5e/PTG source comparison and final live proof: installed D&D5e v14 sources map to the current PTG Character sheet mechanics. D&D5e `dnd5e.mjs` uses `_renderModeToggle()` with `data-action="changeMode"` and `_disableFields()` to leave `.always-interactive` controls usable while making locked `TEXTAREA` fields `readOnly`; PTG mirrors that with `data-ptg-edit-lock-toggle`, `ALLOWED_LOCKED_BUTTON_SELECTOR`, `ALLOWED_READONLY_BUTTON_SELECTOR`, and textarea `readOnly`/`aria-readonly` handling in `module/sheets/sheet-edit-lock.mjs`. D&D5e `character-sidebar.hbs` keeps sidebar/death-tray controls always interactive, swaps editable config buttons for read-only values, and exposes favorite use actions; PTG keeps roll/resource/Creator/item-use/detail controls usable when appropriate while blocking mutation controls in locked or read-only mode. D&D5e inventory controls gate edit/delete behind `@root.editable` while leaving attune/equip/prepare and expand/context controls available; PTG maps the same split through `data-item-action="edit"/"delete"` versus `use`/`toggle-details` and related safe gameplay actions. D&D5e inventory descriptions use collapsible item rows and expanded summaries; PTG item detail rows use structured `.ptg-sheet-detail-body` containers and internal scroll/wrap CSS. Source tests, release validation, and the final Computer Use D&D5e/PTG workflow comparison now cover locked/unlocked mode, header/sidebar/resource readouts, embedded item controls, rich-text detail scrolling, image/title/line containment, and sheet-body scrolling.
- 2026-07-14 #174 creator apply-persistence source slice: the Character Creator apply path now runs through exported `applyCharacterCreatorSelections()`, and `character-creator.test.mjs` proves applying creator selections updates divine identity fields, specialties, Legendary Acts, Spark, Fragments, Skills, Manifestations, starting Truth, starting Bond/Relic attachment Items, and a selected source Occupation Item with its career option. This directly covers the sheet-update mechanics behind the creator; the follow-up Computer Use pass proved the same Random God apply-persistence path in the live Foundry UI.
- 2026-07-14 #174 live Random God apply-persistence proof: Computer Use unlocked `QA Character`, opened the Character Creator, clicked Random God, and observed the pending suggestion `Aurel of the Borrowed Crown, God/dess of Light`. Apply Identity Suggestion filled the editable identity fields (`God/dess of Light`, `Aurel of the Borrowed Crown`, `Guardian of Light`, `the Uninvited Tyrant`, and related symbol/omen/taboo/offering text). A valid creator plan was entered with Athletics 10, Aegis 4, existing `QA Truth`, Spark 1, Fragments 3, and a level-5 Bond named `QA Creator Live Bond`; the apply confirmation listed generated Archetype/Dominion/Theology choices, then the actor sheet persisted `God/dess of Light` on Page 1 and created the level-5 Bond on Page 2. Foundry correctly warned that already-set Archetype, Dominion, and Theology were not overwritten. The sheet was relocked afterward.
- Issue #174 was closed as completed on 2026-07-14 after posting the source/test/live evidence above to GitHub. Any extra no-choice/one-choice live UI variants are now #131 breadth QA rather than a blocker for the character creator issue.
- 2026-07-14 Computer Use smoke pass: no visible Foundry error, warning, failed, or exception notifications were present after the pass. `QA Character` opened with the printed-style `Name` and `God/dess Of` title rows fitted inside their boxes, the sheet unlocked into real editable fields, accepted a reversible live name edit to `QA Character Live Edit`, restored the actor name to `QA Character`, and relocked cleanly. The top action row stayed pared down without the old repeated Common Actions strip. The Character Creator was opened from the sheet and stepped through Occupation, Archetype, Dominion, Theology, Attachments, and Final Touches without clipped step content. The Skill Combo Check dialog scrolled internally and posted a fresh Battle of Fists success card. The Territory Grid and Mortal-Divine Balance windows kept scroll/resize behavior in compact layouts. The 2026-07-14 Foundry `error` log was empty, and the active `debug.2026-07-14.log` warning/error search returned no `level:error`, `level:warn`, `TypeError`, `ReferenceError`, `Uncaught`, or `Exception` entries for the current pass. The test combat encounter was left active with `QA Antagonist` 10 and `QA Character` 6 because ending or deleting it is a destructive Foundry UI action.
- 2026-07-14 Computer Use requested error sweep: entered the active Foundry Electron world as GM and passively inspected the open PTG surfaces, including the God Territory scene, `QA Character`, `Part-Time Gods Premade Items`, and a live `Gear Quality: Aquatic` item sheet. No visible Foundry error, warning, failed, or exception notification appeared; the item sheet rendered readable fields and bounded rich text sections. The active `error.2026-07-14.log` stayed empty, and the parsed `debug.2026-07-14.log` reported 2,610 `info` rows with 0 `warning` and 0 `error` rows through timestamp `2026-07-14 16:53:43`. Supporting checks passed: `npm.cmd run validate` and the full `tests/rules` suite at 74/74.
- 2026-07-14 Computer Use/browser error audit follow-up: the fresh browser-side Foundry console showed no PTG startup errors after reload. The only real warning found during the scene-control pass was Foundry's `SceneControlTool#onClick` deprecation from the PTG scene control tools; `registerTerritoryGridControls()` and `registerGMSetupControls()` now use `onChange`. After another Foundry browser reload, clicking both `PTG Territory GM Interface` and `PTG GM Setup` produced an empty warning/error issue list. `node --check` passed for both patched app files, `npm.cmd run validate` passed, and `npm.cmd test` passed 72/72 outside the sandbox after the known parent-path `EPERM` in sandboxed execution.
- 2026-07-14 Computer Use console audit follow-up: the Electron devtools console initially reported 182 errors and 15 warnings, dominated by repeated Foundry v14 `Cannot read properties of undefined (reading '_source')` errors from pack document updates and PTG's deprecated `renderChatMessage` hook registration. Pack document updates now include the existing compendium document `_id`, and chat actions register only `renderChatMessageHTML`. After clearing the console and reloading Foundry, the console reported 0 errors and 8 warnings, all from the separate Easy Target module using deprecated global `Token`/`TokenLayer` aliases. Opening the PTG Territory Grid after reload did not add PTG errors.
- 2026-07-14 Territory actor import and cell-label live proof: Computer Use used the GM Territory Grid `Import` control to select `QA Character`; Foundry prompted for coordinates for `QA Creator Live Bond`, `QA Lab Landmark`, `QA Therapy Community`, and `QA Wandering Crew`, then created 4 points and 2 influenced cells. Physical drag attempts from the Actors sidebar into the GM Territory window did not trigger a Foundry drop event under automation, so true drag/drop remains open. The import revealed a real layout defect where full point names wrapped into vertical letter stacks inside narrow grid cells; `.ptg-territory-cell` point labels now enforce nowrap ellipsis after the generic wrap rule, `validate-release.mjs` guards `word-break: keep-all`, and a hard-reloaded Foundry recheck showed horizontal `Q...` chips in the cell while full point names stayed readable in the side list. The Electron console remained free of visible PTG stack traces during the recheck; the visible warning count was still from non-PTG module noise already tracked above.
- 2026-07-14 actor drop hardening and runtime proof: `module/util/drop-data.mjs` now tolerates Foundry helper output, JSON fallback, HTML `data-uuid` payloads, and `_id` item drops; the Territory and Mortal-Divine apps listen for drag/drop in capture phase and set copy drop effects without pre-rejecting sparse Electron dragover MIME lists. Computer Use mouse drags from the Actors sidebar still did not deliver Foundry's native HTML5 payload, so this is not hardware-level physical drag proof. The live Electron runtime proof dispatched Foundry-style `DataTransfer` Actor payloads directly into the app surfaces: Mortal-Divine switched its selected actor from `QA Character` to `Test`, and Territory produced the expected `QA Character has no new territory-ready...` warning against the already-imported character. Focused drop/source tests passed, `npm.cmd run validate` passed, and the full test suite passed 74/74 outside the sandbox after the expected parent-path `EPERM`.
- 2026-07-15 #165 macro/API replacement review: `tests/rules/source-data.test.mjs` now explicitly proves the integrated `game.ptg.territory` API remains the primary Territory entry point, `restoreIntegratedModuleApis()` preserves and reasserts it after competing module overrides, ready flow still calls `maybeOpenTerritoryInterfaceOnReady()`, and `PTG: Create Territory Scene` plus `PTG: Territory Controls` remain compatibility launchers pointing to the native Unified Territory interface. `node --test tests\rules\source-data.test.mjs` passed 25/25 outside the sandbox after the expected Foundry data-path `EPERM`, and `npm.cmd run validate` passed with 8 macros.
- 2026-07-15 #165 Territory drop resolver follow-up: the GM Territory app now resolves Actor UUID, Token UUID, Actor ID, and Token-typed ID payloads through exported `territoryActorFromDropData()` before deciding whether to show a warning. Focused source-data tests passed 29/29 outside the sandbox after the expected Foundry data-path `EPERM`, `node --check module\apps\territory-grid-app.mjs` passed, and `npm.cmd run validate` passed. A live migrated-clone F5 reload kept the locked-pack startup warning fixed, but visible actor-directory drags from `Character` and `Character (2)` into the Territory grid/points panel still did not deliver a native Foundry payload or change the `3 points` count under automation. A slower Windows-level drag helper was abandoned after local security software blocked the temporary helper DLL before a gesture could be produced.
- 2026-07-14 live Armor item/drop and equipped-armor proof: Computer Use opened `QA Character` in the Electron client, unlocked the sheet, created or updated world Armor `QA Runtime Armor Drop` with Rating 2, Equipped true, and Fire Proof 2, then dispatched a Foundry-style `DataTransfer` Item payload onto the Character sheet. The live readback showed `QA Character` had 1 owned copy, equipped true, rating 2, Health 5, and derived Armor 2. The damage chat card for reason `QA runtime armor drop proof` showed Health Damage against `QA Character`, tag `fire`, Raw Damage 6, Armor -2, Tag Armor -2, Applied 2, and `Health: 7 -> 5`. This proves the runtime Item drop handler for Armor payloads and the equipped-armor reduction path in Foundry; physical sidebar-to-sheet item dragging remains under the broader #131 drag/drop matrix.
- 2026-07-14 Computer Use full-system error sweep: the first pass exposed a real live Territory scene defect where the managed Legend drawing still sat at `x=0,y=100`, plus the locked character sheet disabled the Creator workflow button. The fix makes Creator an intentional locked-sheet workflow button, moves Territory backgrounds to v14 `levels[0].background`, removes deprecated scene background reads from the GM prompt, and repairs stale managed Territory drawing geometry on GM open without resetting user background choices. After Foundry reload, the post-fix live check opened Creator from locked `QA Character` with a readable 760x720 six-step dialog (`overflow-y: auto`, 86 controls), applied/restored a Territory background image/color, verified Legend no longer overlaps row/column labels, verified foreground overlay sort `5000..5023`, and captured 0 PTG console errors/warnings.
- 2026-07-14 Computer Use DevTools smoke: the live Electron client loaded `part-time-gods` 0.1.0 in Foundry 14.364 as `Gamemaster`; the Foundry structured log scan found 2,813 debug entries, 0 warning/error/deprecation entries, 0 PTG non-info entries, and an empty `error.2026-07-14.log`. A live console smoke script rendered representative Character/Antagonist/Pantheon sheets, found the Character Creator button, rendered premade Item `Game Face`, rendered rules journal `07. Opposition`, opened Territory, Mortal-Divine Balance, and GM Setup APIs, and verified compendium index counts of 40 Character Creation, 600 Premade Items, 9 Rules Reference, 1 Map, 91 Roll Tables, 8 Macros, and 62 Opposition Actors. The smoke harness used the wrong combat opener name and a raw synchronous `Roll` check; follow-up source review confirmed the public combat opener is `openPTGCombatControls` and exposed a real PTG dice-engine risk where `PTGDiceEngine.rollPool()` still preferred `Roll.evaluateSync()`. After reloading, the live console exposed the next Roll API issue: Foundry rejected `Roll#evaluate({ async: true })` as a removed option. `PTGDiceEngine.rollPool()` and `rollPTGStatblockPool()` now use plain awaited `Roll#evaluate()` with focused dice and combat regression coverage. Computer Use then timed out before the final post-patch console rerun, so one clean live dice smoke remains.
- 2026-07-14 true non-GM player-client proof: a temporary headless Edge profile logged in through the normal `/join` POST as role-1 `Player3` with blank password, assigned character `Test`, and `isGM: false`. The client loaded the active world with `game.ready` and `canvas.ready`, did not expose `PTG GM Setup`, `Combat Controls`, or `Opposition Builder` text, and `game.partTimeGods.openTerritoryInterface({ notify: false })` routed to the player-facing `God Territory Grid` scene instead of the GM app. The player canvas showed `Scene.vZ5MWDgsubN90MZW`, board size `756x454`, and fit scale `0.325`. The player Balance tracker rendered `Your Mortal-Divine balance`, showed `Test`, did not show `QA Character`, and did not expose Add Character, Remove, or Custom Adjustment controls. A compact rerun exposed that the separate legacy `ptg-balance-tracker` module overwrote `game.partTimeGods.openMortalDivineBalanceTracker` with a GM-only opener while `game.ptg.balance.open` remained correct; `part-time-gods.js` now reasserts the integrated system API after ready hooks, and the template marks the non-GM body with `data-balance-player-view`. Fresh compact `Player3` proof then showed `game.partTimeGods` and `game.ptg.balance` using the same player-safe opener, returned a live app, rendered the marked player view at `420x360`, kept only `Test` visible, hid GM management controls, and produced 0 console errors/warnings.
- 2026-07-11 Character ability readability source slice, expanded and live-verified 2026-07-12: Character sheet Blessing/Curse/Relic/Truth detail rows now use structured label/body containers, compact action groups, and a shared detail formatter that decodes escaped simple HTML into readable prose while stripping unsafe or layout-breaking markup. Clear Blessing mechanics now flow from character-choice source data into generated premade Item automation for damage prevention, damage-cost rerolls, damage-threshold Pantheon Dice, healing, Conditions, rolls, bonuses, and resource changes; Character sheet detail rows render those hooks as readable prose instead of raw metadata. Unit coverage verifies escaped HTML, plain angle-bracket text, unsafe markup handling, readable hook prose, and generated Blessing hooks for `Made of Sturdy Stuff`, `Fate's Compliance`, and `Martyrdom`. Release validation guards the detail formatter, action groups, detail body CSS, and source-data item integrity. Live Foundry proof at the default browser viewport opened `QA Character`, resized the sheet from 900px to about 783px wide, expanded the Outsiders Guide `Borrowed Lives` Blessing, and confirmed compact `Hide`/`Edit` controls stayed inside the row, player-facing text rendered without raw markup, the long rules body scrolled internally (`scrollHeight 398`, `scrollTop 320`; at narrower width `scrollHeight 353`, `scrollTop 305`), and the Page 2 panel did not move during detail-body scrolling. The same pass opened the `Borrowed Lives` backend Item sheet, confirmed Effect/Notes/Rules Explanation fields stayed reachable inside the shared `.ptg-item-sheet` scroll body (`scrollHeight 1297`, `scrollTop 620`), then expanded `QA Truth` as a non-Blessing representative row and confirmed its Details/Use/Edit/Delete controls and detail text remained contained at the narrowed width.
- 2026-07-08 browser continuation joined the same active world through `http://127.0.0.1:30000/join` as `Player2`; the live page reached `/game`, showed `Player2`, and the visible non-GM UI did not contain `PTG GM Setup`, `Combat Controls`, `Territory Controls`, `Opposition Builder`, or `Mortal-Divine Balance`. Generic Foundry controls, chat cards, and sidebar tabs were visible. A browser click attempt against old chat-card `Health Damage` buttons could not be completed because the in-app browser click translation missed the Foundry sidebar coordinate, so live non-owner damage denial remains unresolved.
- 2026-07-11 browser continuation again joined the active world as `Player2`; the live DOM reached `/game`, showed `Player2` and `Gamemaster [GM]` connected, and did not show `PTG GM Setup`, `Combat Controls`, `Territory Controls`, `Opposition Builder`, or `Mortal-Divine Balance` in the visible non-GM UI. The old chat-card `Health Damage` buttons were visible in the DOM, but in-app browser coordinate translation still missed the Foundry sidebar target, so non-owner damage denial remains covered by automated tests but not live-click proven.
- The live `PTG Combat Controls` Actor/Attacker selector exposed only `Encounter helper only` during this continuation. The God Territory Grid scene was viewed and its 10x10 grid was visible, but actor-sidebar token placement did not create tokens under automation, so action marker and combat-control healing proof still require adding actual actor combatants to the encounter.
- 2026-07-08 desktop continuation: `QA Armor Live Proof` was created through the Items sidebar as an Armor item, configured to Rating 2, and marked Equipped. Repeated sidebar-to-sheet drag/drop attempts from the item icon and item row text did not attach it to `QA Character` in the automated desktop session. The 2026-07-14 runtime `DataTransfer` proof supersedes the equipped-armor reduction gap, but physical sidebar-to-sheet item dragging remains unresolved.
- 2026-07-08 maps-pack audit: copied `packs/maps` without the live LevelDB `LOCK` file and inspected it with `classic-level`. The shipped pack contained 1 Scene, 24 Drawing child rows, and 24 stale Drawing child rows with empty `author` and `shape.type` set to `rectangle`; the active world scene copy was already clean, so the defect is isolated to stale compendium pack data. Source generation now emits fallback author `0000000000000000` and v14 rectangle shape code `r`, release validation audits premade Scene drawing schema, and automated source-data tests verify both v14-ready source drawings and stale managed Drawing refresh behavior. Rebuilding the actual `packs/maps` LevelDB remains pending until Foundry is closed and releases the pack lock.

## Unresolved Coverage Gaps

| Gap | Impact | Next Action |
| --- | --- | --- |
| Foundry install-by-manifest UI has not been run in a clean install target. | The local `0.1.0` release ZIP and `dist\system.json` now build cleanly after Foundry closes, but the live GitHub Release manifest still advertises `0.0.2`, and the `0.1.0` ZIP asset is missing. The Foundry installer path itself is not proven for `0.1.0`. | Merge/push this release state to `main`, run the `Publish Release Assets` workflow by pushing tag `v0.1.0` or dispatching it from `main`, rerun `npm.cmd run check:release-assets`, then use a separate Foundry data path or temporarily remove the local system, install from the GitHub Release manifest URL, and open a world. |
| Premade Item and journal audit is complete for #172. | Source-data audit proves all created Item source docs are true Items with valid types/folders/source IDs and no journal-style leaks. The 2026-07-14 rebuild and Computer Use recheck proved the retired Battle folders are gone from live Premade Items and Battle of Fists/Wits are readable rules-reference pages, not Items. Follow-up live checks proved Otherworld travel is journal procedure content in `04. Divine Expressions`, Premade Items search exposes only legitimate `Otherworldly Sight`, and Character Creation exposes representative structured choice Items. | #172 is closed; keep any future individual compendium spot checks under #131 production breadth QA. |
| Extra Random God live variant matrix is optional breadth coverage. | Source and automated coverage verify the manual/apply fields, no-overwrite preview/apply flow, source roll-table path logging, missing-choice fallbacks, identity text influenced by selected Occupation, Archetype, Dominion, and Theology, the #174 matrix for two Archetype/Dominion combinations plus no-choice and one-choice cases, and direct creator apply persistence for final sheet fields and created Items. The 2026-07-14 Computer Use pass now proves the core live path: generate Random God suggestion, Apply Identity Suggestion, confirm a valid creator plan, persist `God/dess of Light` on the actor sheet, and create a level-5 starting Bond. | #174 is closed; keep any no-choice/one-choice live variant spot checks under #131 breadth QA only. |
| Drag/drop item matrix is partially live-proven. | Shared drop-data resolution is covered for Foundry Item source shapes, HTML `data-uuid`, and `_id` payloads, and the character sheet `_onDrop` path is covered for every item type named in the #131 drag/drop matrix plus typed-section rejection. The 2026-07-14 Electron runtime proof created an owned Armor item from a Foundry-style `DataTransfer` Item payload, but physical sidebar-to-sheet mouse dragging and the other player-facing item types are still not proven live. On 2026-07-16, Computer Use opened `Part-Time Gods Premade Items -> Conditions` and attempted physical drags of `Afraid` into the unlocked Character sheet; no owned Condition appeared and no warning appeared. `check:dragdrop-evidence` currently reports 0/15 physical rows passed. | Drag/drop every row in `docs/qa/manual-dragdrop-matrix.md`, record actor/item names and warning text, then rerun `npm.cmd run check:dragdrop-evidence` until all `item-*` rows pass. |
| Equipped armor reduction is live-proven. | The armor toggle exists, `applyDamageToActor()` is covered for equipped armor plus matching proof quality, sheet `_onDrop` is covered for Armor gear, and the 2026-07-14 Electron runtime proof showed Armor -2 plus Fire Proof tag armor -2 reducing raw fire Health damage 6 to Applied 2 with `Health: 7 -> 5`. | No separate equipped-armor action remains; keep any physical Armor drag follow-up under the broader drag/drop item matrix. |
| Active combat helper flow is live-proven for #171. | Combat Controls opens from GM Setup in an active encounter, automated tests cover collection-backed dialog rendering, initiative updates, selected-combatant action markers, selected Psyche damage, object-resource healing, scalar statblock Health recovery from `0 -> 1`, and collection-backed actor Item handling for weapon/armor helper paths. The 2026-07-12 live pass proves source-backed initiative can update native Combat Tracker values and start turn order. The 2026-07-14 Computer Use pass additionally proves Foundry's native Combat Tracker initiative path for `QA Character` and `QA Antagonist`; the follow-up pass posted Quick Action, Health damage `7 -> 6`, Psyche damage `8 -> 7`, Health healing `6 -> 7`, and Psyche healing `7 -> 8` through the GM panel path. | No remaining #171 combat-helper action/damage/healing follow-up; keep equipped armor proof under the separate armor row. |
| Permission boundaries are live-proven for GM/owner/observer/non-owner role slices. | Automated tests verify non-GM Combat Controls denial and non-owner damage denial. Live browser continuation verified `Player2` does not see the PTG GM setup/control surfaces. The 2026-07-14 headless Edge proof verified role-1 `Player3` can join as a true non-GM owner of `Test`, sees no GM setup/control text, and receives only owned-character Balance data. The follow-up in-app browser proof verified Player3-owned Character sheet default-unlocked and locked-sheet behavior in the live UI without chat commands. The 2026-07-15 Player4 browser proof verified a true non-GM/non-owner client renders `/game`, sees no Actor entries, cannot apply QA Character Health damage from a visible chat-card action, posts no new chat message, and receives `No editable actors are available for applying damage.` The later Player4 observer proof changed only `QA Character` ownership to OBSERVER, verified the actor became visible but the sheet remained read-only, kept tabs/details usable, blocked mutation/play-roll controls, and kept long detail text internally scrollable. | Permission role slice is complete for #131; keep future role regressions under focused bug reports or #131 breadth checks. |
| Territory scene image/background/overlay behavior is complete for #176; integrated actor drag remains under #165/#131. | Source and validator coverage now enforce the player Territory Scene route, fit-to-canvas math, GM-only mutation guards, Character actor import, background update payloads, non-overlapping Legend geometry, integrated `game.ptg.territory` API restoration, ready auto-open flow, compatibility-only Territory macros, and Actor/Token drop payload resolution through `territoryActorFromDropData()`. The 2026-07-14 Computer Use pass proves the GM interface is resizable/scrollable, the Background dialog is readable, `View` frames the scene, the overlay fits the canvas, the GM Import control can create 4 `QA Character` territory points without grid-cell label clipping after the ellipsis fix, a runtime Foundry `DataTransfer` drop reaches the Territory handler, and the post-fix live check applies/restores a v14 level-backed background image/color while keeping the repaired Legend and foreground overlay intact. The headless `Player3` proof now proves the true non-GM player route opens only the Territory Scene/canvas and not the GM interface. Issue #176 was closed on 2026-07-14 after a fresh evidence comment and focused source-data validation. The 2026-07-15 Setup-to-world GM pass reconfirmed integrated Territory/Balancer auto-open and visible imported Territory data, but actor-directory drags into the GM Territory panel/grid did not deliver a native Foundry Actor payload or change the point count in either the QA world or the migrated clone. A later Computer Use attempt from `Character` / `Character (2)` into the GM Territory grid and points panel likewise left the app at `3 points` / `2 influenced cells`, so hardware-level physical Character actor drag remains unproven and is represented by `actor-territory-character` in `docs/qa/manual-dragdrop-matrix.md`. | Run the `actor-territory-character` row manually and rerun `npm.cmd run check:dragdrop-evidence`; keep future territory breadth under #165/#131. |
| Mortal-Divine `Show Player Bar` owner/non-owner path is live-proven for #182. | Source and validator coverage now enforce the socket registration, GM show-player action, owner-only targeting without stale active-flag dependence, compact player-bar template, image fallback rendering, and automatic refresh attempts after later balance changes. The 2026-07-15 migrated-world browser pass confirmed Player3 owns `Character` and the GM tab shows `Sent Character's balance bar to Player3.` It also fixed the boolean `data-balance-action` delegated-click regression. On 2026-07-16, the restarted Foundry host joined as GM in Electron, normal Edge joined as owning Player3, and Edge InPrivate joined as non-owning Player2. After the Player3 balance window was closed, GM `Show Player Bar` reopened a compact player-only Mortal-Divine window for Player3 with `GM-shared character balance`, `Character`, `Balanced (-1)`, and the balance meter. The same resend while Player2 was connected still targeted Player3; Player2 showed only `User Configuration: Player2` and had no Mortal-Divine window, no balance meter, and no GM controls. GitHub evidence comment `4993637128` was posted and #182 was closed as completed on 2026-07-16. | Keep future Balance tracker breadth under #131 or a new bug. |
| Mortal-Divine tracker player-facing resize behavior is live-proven. | Source and validator coverage enforce auto-open settings, GM tracked roster management, player-owned filtering, Character actor drop-data resolution for GM roster import, scroll/resize CSS, container-width compact layout, and internally scrolling GM roster behavior for #169/#170. The 2026-07-12 live browser pass proved the GM scene-control path, dropdown-add roster behavior, open party-card controls, normal-size detail scrolling, and compact-size body scrolling. The 2026-07-14 Computer Use pass again proved GM auto-open plus scroll/resize behavior in compact layout, and the follow-up runtime Foundry `DataTransfer` drop switched the selected GM actor to `Test`. The headless `Player3` proof verifies the true player-facing view shows only owned character `Test` and hides GM roster management controls. The compact `Player3` proof verified the public integrated API after the legacy-module compatibility fix and rendered the player view at `420x360` with `overflow-y: auto` on the player body. | Run manual/hardware-level actor drag into the GM roster if Computer Use keeps failing to deliver Foundry's payload. |
| Item sheet spacing and collapse behavior is complete for #168. | Source and validator coverage now enforce readable field geometry, long-field internal scrolling, fallback artwork, and collapsible rich text sections for shared Item sheets and legacy fallback templates. Live Foundry proof confirms Armor and Truth sheets stay inside normal and compact viewports, and the 2026-07-14 all-type sweep proved every registered Item type at compact `520x520` with internal scrolling, no bad controls/labels/editors, long text contained, and fallback media loaded. | #168 is closed; keep future item-sheet regressions under #131 production breadth QA or a new focused issue. |
| D&D5e-inspired Character sheet comparison is complete for #177. | #177 source work now covers Character-only edit/read mode, allowed gameplay controls, combat presets, structured readable item details, tab scroll persistence, D&D5e-style readonly textareas, player-owner editability, observer/non-owner read-only behavior, and removal of the duplicated Common Actions header strip. 2026-07-15 source comparison maps D&D5e mode toggle, `.always-interactive` controls, readonly textarea behavior, editable-only config/edit/delete controls, non-edit gameplay controls, favorites/use actions, and collapsible inventory descriptions to PTG edit-lock toggles, allowed locked/read-only controls, readonly textareas, item action gating, roll/resource/Creator actions, and `.ptg-sheet-detail-body` rows. Live proof covers GM, player-owner, observer, and non-owner permission slices. Final Computer Use side-by-side proof opened D&D5e `Friar Briar`, verified Play/Edit mode, header/sidebar/resource readouts, embedded Feature controls, and expanded details, then opened PTG `QA Character`, verified locked/unlocked mode, live editable field writes/restores, contained title/image/line/resource layout, Page 2 embedded sections, expanded `Borrowed Lives` internal rich-text scrolling, and lower-page scroll behavior. GitHub evidence comment `4985544256` was posted and #177 was closed as completed. | No remaining #177 action; keep future Character sheet regressions under #131 production QA or a new focused issue. |
| Existing-world migration is live-proven. | Automated coverage verifies legacy flag preservation, owned Item conversion, duplicate-skip behavior, and forced reruns. The 2026-07-15 migrated snapshot cloned older world `tester` (`systemVersion: 0.0.2`), ran Foundry's `0.0.2 -> 0.1.0` migration with backup, joined as GM, and live-reloaded after the protected-pack startup fix with no red locked-compendium warning. | No remaining migration snapshot action for #131; keep future migration regressions under focused bug reports or release QA. |

## Production Blockers / Defects

One product defect was observed and fixed during this pass: active `Combat Controls` could fail in Foundry v14 because `combat.combatants` is collection-backed and was treated as an array in `module/combat/ptg-combat.mjs`. The fix normalizes combatants before dialog rendering, initiative iteration, and round reset updates; the live retest posted the Round and Turn Sequence card. No separate blocker issue was filed because the defect was fixed in this pass.

A second product defect was observed and fixed during the 2026-07-15 Computer Use clean F5 pass: Pantheon Table Tools/Members and Antagonist Combat Actions could render as thin unreadable strips because non-editor action panels used shared `.ptg-editor-section` flex/overflow behavior. Those panels now use `.ptg-panel-section`, compact actor sheet children are guarded against shrinking, and release validation checks the templates/CSS. Clean F5 retest proved the panels are visible and Antagonist Roll Attack posts a chat card.

Another UI defect was observed and fixed during the desktop continuation: Character Creator popout dialogs could inherit low-contrast or non-opaque Foundry window styling, making the creator workflow hard to read. The main creator, validation, and apply-confirm dialogs now use the shared readable sheet-dialog shell and explicit paper backgrounds.

The 2026-07-15 Player4 browser creator pass exposed two creator defects: hidden duplicate dependent options could be selected through duplicate values, and confirmed source-choice application could continue into extra attachment/Dominion prompts that looked like no update if unanswered. The creator now parent-scopes Archetype and Occupation career option values, disables hidden options, selects visible dependent options explicitly, and passes non-interactive attachment definitions and Dominion defaults during creator application. Focused creator tests passed, and the hard-reload Player4 retest confirmed the promptless source-choice path live with no extra follow-up dialogs.

The 2026-07-15 migrated-world snapshot pass exposed a startup defect in ready-time compendium auto-population: after the older `tester` world migrated to `0.1.0`, Foundry displayed red locked-pack notifications for `part-time-gods.premade-items`. Ready-time population now skips protected package packs before any write call by passing `skipLockedPacks: true`, while the manual GM Setup populate action remains available for deliberate pack updates. A live F5 reload of the migrated clone showed only the normal blue already-populated message and no delayed red locked-pack warning. The same pass also produced two `world.json` parse errors from the first cloned snapshot manifest because it was saved with a UTF-8 BOM; the clone manifest was re-saved without BOM before the migration proof continued, so those log entries are clone setup noise rather than a PTG runtime migration failure.

The maps compendium pack has stale embedded Drawing rows in the local checkout while Foundry is open. Source generation, release validation, compendium refresh logic, and Legend label geometry have been fixed, but the shipped `packs/maps` LevelDB files cannot be rebuilt until the running Foundry processes release the pack lock.

The rules-reference and Premade Items stale Battle blocker was repaired and rechecked on 2026-07-14. The first Computer Use pass opened `Part-Time Gods Rules Reference -> 06. Divine Battles` and exposed repeated `Timing, Initiative, and Turns` pages plus retired Battle folders in Premade Items; runtime seeding now explicitly refreshes managed `JournalEntryPage` rows and removes retired slugged Battle folders. After targeted pack rebuild, direct LevelDB verification found the expected Battle of Fists/Wits rules pages, no duplicate page names, and no retired Battle folders, and the follow-up live Electron pass confirmed the same behavior in Foundry. Keep the broader premade item/journal spot-check gap open.

The 2026-07-14 Combat Controls continuation exposed a dialog-layout defect: at the default desktop dialog size, the form body could consume the footer area and hide the `Post` button. `module/combat/ptg-combat.mjs` now opens Combat Controls with `ptg-combat-dialog-window` and a 700x680 starting size, while `styles/part-time-gods.css` bounds the content area, keeps the footer fixed, and gives the inner form a stable scrollbar. The live reload confirmed the footer remained visible and all #171 action/damage/healing posts succeeded.

The 2026-07-14 error-audit follow-up exposed and fixed a Foundry v14 deprecation warning in the PTG scene controls: `SceneControlTool#onClick is deprecated in favor of SceneControlTool#onChange`. The GM Setup and Territory scene-control tools now use `onChange`, and a fresh browser reload/click pass confirmed both controls run with no warning or error entries.

The 2026-07-14 Computer Use console audit exposed and fixed a PTG startup/runtime console defect: compendium population updated pack documents without carrying the existing `_id`, which made Foundry v14's document update handler attempt to resolve an undefined document source and repeat `Cannot read properties of undefined (reading '_source')`. `module/data/premade-compendiums.mjs` now preserves `_id` on pack document updates, `module/chat/chat-actions.mjs` no longer registers the deprecated `renderChatMessage` hook, and the post-reload Electron console showed 0 PTG errors. The remaining 8 warnings are from the separate Easy Target module's deprecated global `Token`/`TokenLayer` access.

The 2026-07-14 Computer Use error sweep exposed and fixed two PTG live defects: the Character Creator button was disabled while the sheet was locked, blocking the intended workflow, and the existing Territory scene still had stale Legend drawing geometry plus deprecated scene background update/read paths. `module/sheets/sheet-edit-lock.mjs` now allows the Creator workflow while locked, `module/apps/territory-grid-app.mjs` updates/restores v14 `levels[0].background`, repairs stale managed drawing geometry on GM open, and keeps overlay drawings foregrounded, and `module/data/premade-scenes.mjs` now emits v14 level-backed scene background data. Focused tests and release validation passed, and the post-fix live Electron check captured 0 PTG console errors/warnings.

The 2026-07-14 Computer Use DevTools smoke exposed and fixed PTG Roll API compatibility defects: `PTGDiceEngine.rollPool()` could not use `Roll.evaluateSync()` for random dice, and the live post-reload console rejected the removed `Roll#evaluate({ async: true })` option. `module/dice/ptg-dice-engine.mjs` and `rollPTGStatblockPool()` now use plain awaited `Roll#evaluate()`, and `tests/rules/dice.test.mjs` includes a regression where sync evaluation exists but throws and `evaluate()` must receive no removed async option. Focused validation passed: dice 6/6 and combat controls 8/8. A final post-patch Electron console rerun is still needed because Computer Use timed out before it could submit the loader.

The 2026-07-08 desktop continuation confirmed chat-command aliases are not part of the desired workflow, and `PTG GM Setup -> Combat Controls` opens the supported dialog. The same continuation could not complete actor/token or item/sidebar drag/drop through desktop automation; direct automated coverage now exercises the relevant Combat Controls, drop-data, and character-sheet drop handlers, but the live UI gates remain unresolved. Issue #131 cannot close until the unresolved coverage gaps above are completed or explicitly deferred.

## Release Gate

Do not tag a production release from this QA pass yet. Automated checks are green and many runtime paths pass, but #131 still has unverified install-by-manifest and physical drag/drop breadth coverage.
