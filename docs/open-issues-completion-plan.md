# Open Issues Completion Plan

Last updated: 2026-07-16

Current live GitHub state after this pass: 2 open issues in `VentysGrimm/Part-Time-Gods`: #165 and #131. Issue #182 was closed as completed on 2026-07-16 after posting the true GM/owning-player/non-owner `Show Player Bar` proof. Issue #177 was closed as completed on 2026-07-15 after posting final live D&D5e/PTG side-by-side Character sheet workflow proof. Issue #179 was closed as completed on 2026-07-15 after posting fresh Setup-to-world live invalid-apply/review/corrected-retry creator proof. Issues #181 and #180 were closed as completed on 2026-07-15 after posting copied-pack audit, live Electron compendium proof, no-PTG-package-warning evidence, empty runtime error log evidence, and passing release validation. Issues #176 and #171 were closed as completed on 2026-07-14 after posting fresh evidence comments. Issue #170 was closed as completed on 2026-07-14 after true non-GM player-client Balance resize/data-isolation proof and the integrated API compatibility fix. Issue #174 was closed as completed on 2026-07-14 after source/test coverage plus live Random God apply-persistence proof. Issue #172 was closed as completed on 2026-07-14 after full source audit plus representative live compendium spot checks. Issue #168 was closed as completed on 2026-07-14 after the all-registered-item-sheet resize/scroll/media live sweep.

Validation baseline for this pass:

- `npm.cmd run validate`: passed.
- Focused #180/#181 source conversion recheck passed on 2026-07-14: `node --test tests\rules\source-data.test.mjs` 24/24, `node scripts\audit-premade-items.mjs` reported 640 created Items with 0 audit issues, and `npm.cmd run validate` reported 600 premade Items, 9 rules journals, and 91 RollTables.
- 2026-07-15 #180/#181 copied-pack LevelDB audit found 600 Premade Items, 0 stale Manifestation Application/Critical Failure/Gear Quality rules-as-items, 9 Rules Reference journals, 56 rules pages, and all expected generated pages for Manifestation Applications, Critical Failure Effects, and Gear Qualities.
- Focused #179 character creator failed-review recovery recheck passed on 2026-07-14: `node --test tests\rules\character-creator.test.mjs` 9/9, the then-current dice slice 5/5, `node --test tests\rules\source-data.test.mjs` 24/24, `npm.cmd test`, and `npm.cmd run validate` passed. After the later Roll API, #177 observer/non-owner permission regressions, #131 drag/drop coverage, and #179 corrected-retry mutation fix, the full `npm.cmd test` suite passes 87/87. Syntax checks passed for the touched creator, dice, premade item, validator, and test modules.
- Follow-up Computer Use DevTools smoke on 2026-07-14 exposed Foundry v14 Roll API compatibility risks: `PTGDiceEngine.rollPool()` could not use synchronous random-dice evaluation, and the post-reload live console rejected the removed `Roll#evaluate({ async: true })` option. `module/dice/ptg-dice-engine.mjs` and the statblock helper in `module/combat/ptg-combat.mjs` now use plain awaited `Roll#evaluate()`. Focused tests pass: `node --test tests\rules\dice.test.mjs` 6/6 and `node --test tests\rules\combat-controls.test.mjs` 8/8.
- 2026-07-15 focused #177 permission recheck passed outside the sandbox after the expected Foundry data-path `EPERM`: `node --test tests\rules\sheet-edit-lock.test.mjs` 7/7.
- 2026-07-15 focused #165 macro/API replacement review passed outside the sandbox after the expected Foundry data-path `EPERM`: `node --test tests\rules\source-data.test.mjs` 25/25. `npm.cmd run validate` also passed with 62 actors, 600 premade Items, 40 choices, 9 journals, 91 roll tables, 1 scene, 8 macros, and stable source keys.
- 2026-07-15 focused #131 drag/drop and migration source-breadth recheck passed outside the sandbox after the expected Foundry data-path `EPERM`: `node --test tests\rules\drop-data.test.mjs tests\rules\character-sheet-drop.test.mjs tests\rules\migration.test.mjs` 7/7. `tests/rules/character-sheet-drop.test.mjs` now covers the direct-copy Character sheet drop matrix for core choices, abilities, attachments, conditions, powers, gear-quality documents, weapons, and armor; typed-section rejection; and Attachment of Choice conversion into a concrete owned attachment. `tests/rules/migration.test.mjs` now also proves completed canonical embedded-item migrations are skipped unless explicitly forced, and forced reruns create/clear only the requested legacy fields. `npm.cmd run validate` also passed with 62 actors, 600 premade Items, 40 choices, 9 journals, 91 roll tables, 1 scene, 8 macros, and stable source keys.
- 2026-07-15 focused #179/#131 creator follow-up recheck passed outside the sandbox after the expected Foundry data-path `EPERM`: `node --test tests\rules\character-creator.test.mjs` 12/12. `applyCharacterCreatorSelections()` now applies selected source choices before creator budget/field writes, so a failed source application can reopen Review without leaving already-added Skill or Manifestation points that would double on corrected retry; it also passes non-interactive attachment definitions and Dominion defaults to source-choice application so a confirmed Creator apply does not disappear into extra follow-up prompts after the preview. Occupation career option values are now parent-scoped like Archetype dependent options, preventing hidden duplicate values from selecting the wrong visible label while keeping legacy `career:attachment` values parseable. Related source/drop coverage passed 38/38, full `npm.cmd test` passed 87/87 before this slice, syntax checks passed for the touched creator/actor modules, and `npm.cmd run validate` passed with stable source keys.
- 2026-07-15 Computer Use clean F5 reload/system-smoke found compact actor action panels visually collapsed on Pantheon and Antagonist sheets. Fixed by moving non-rich-text actor action/member panels to `ptg-panel-section`, adding no-shrink CSS, and adding release-validator guards. Clean post-F5 retest proved Territory and Mortal-Divine auto-open, Pantheon Table Tools/Members visible, Antagonist Combat Actions visible, and Antagonist Roll Attack posted `QA Antagonist: Attack` with `Successes: 0`. `error.2026-07-15.log` stayed 0 bytes, `npm.cmd test` passed 87/87, and `npm.cmd run validate` passed.
- `npm.cmd test`: passed, 83 tests.
- Focused 2026-07-14 closure rechecks passed: `node --test tests\rules\source-data.test.mjs` 22/22 for #176 territory background/overlay/legend behavior, and `node --test tests\rules\combat-controls.test.mjs` 8/8 for #171 combat controls behavior.
- Focused coverage passed for character creator attachments and apply persistence, player-owned sheet editability, source-data cleanup, native combat tracker hooks, territory model/background helpers, balance tracker visibility, item-sheet readability helpers, retired Battle folder cleanup, rules JournalEntryPage refresh, tolerant Foundry drop-data parsing including HTML `data-uuid` item links, and Foundry v14-safe compendium update IDs.
- Live Foundry GUI smoke is partially proven through Computer Use in the Electron client: the QA world opened as `Gamemaster`, `QA Character` opened cleanly, locked title rows fitted inside their printed boxes, unlock exposed editable sheet fields and accepted a reversible name edit, the six-step Creator dialog was readable, Random God generated an identity suggestion and the creator apply path persisted `God/dess of Light` plus a level-5 `QA Creator Live Bond`, GM Territory and Mortal-Divine windows resized/scrolled internally, the Territory Import control converted `QA Character` attachments into 4 grid points, compact Territory cell labels were rechecked after a hard reload as horizontal ellipsized chips, runtime Foundry `DataTransfer` drop events reached the Mortal-Divine and Territory app drop handlers, a runtime Item drop created owned Armor `QA Runtime Armor Drop` on `QA Character`, equipped Armor plus Fire Proof reduced raw fire Health damage 6 to Applied 2 with `Health: 7 -> 5`, all 17 registered Item sheet types passed the compact resize/scroll/media sweep, the skill-combo dialog scrolled and posted a fresh Battle of Fists chat card, native Combat Tracker initiative rolled for `QA Antagonist` and `QA Character`, GM Combat Controls posted action, damage, and healing cards from the active encounter, the repaired Premade Items and Rules Reference compendia matched source, and the post-reload Electron console plus 2026-07-14 Foundry runtime logs showed 0 PTG errors/warnings. The latest DevTools smoke additionally rendered representative actor, item, journal, Territory, Balance, and GM Setup surfaces, then exposed the Roll API compatibility defect above. A final post-patch console rerun was blocked by the Computer Use helper timing out, so the Roll fix is source/test-proven but still needs one clean live dice smoke.
- 2026-07-15 Computer Use GM continuation re-entered the PTG QA world and found no blocking PTG error modal. Setup Package Warnings (6) were traced to other installed systems (`blades-in-the-dark`, `lone-wolf-fists`, `mutants-and-masterminds-3e`, `vaesen`, and `wfrp4e`; Lone Wolf Fists accounts for two warnings because it also has unknown manifest keys). The pass reopened `QA Character`, verified the Creator steps and final-step inner scrolling, confirmed the native Combat Tracker still held Round 1 with `QA Antagonist` initiative 10 and `QA Character` initiative 6, opened a representative QA Item sheet, expanded Part Time Gods compendium packs, and opened `Rules Reference -> 06. Divine Battles` with Battle of Fists/Wits pages as journal rules content. `error.2026-07-15.log` was empty at the time of the pass.
- 2026-07-15 Computer Use setup/package sweep and compendium recheck confirmed the Package Warnings panel still has 6 warnings, all from non-PTG installed systems: `blades-in-the-dark`, `lone-wolf-fists` twice, `mutants-and-masterminds-3e`, `vaesen`, and `wfrp4e`. No Part-Time Gods package warning appeared. The live `Part-Time Gods Premade Items` pack returned no entries for exact stale rules terms `Manifestation Applications`, `Critical Failure`, or `Gear Qualities`. The live `Part-Time Gods Rules Reference` rendered `04. Divine Expressions -> Manifestation Applications: Aegis, Beckon, Journey, Minion, and Oracle`, `05. Dice, Skills, and Resources -> Critical Failure Effects`, and `06. Divine Battles -> Gear Qualities: Armor and General` / `Gear Qualities: Weapon`. `error.2026-07-15.log` remained 0 bytes after the pass and `npm.cmd run validate` passed.
- 2026-07-15 Computer Use #179 proof started from Foundry Setup, joined `PTG v14 QA Smoke Test` as `Gamemaster`, created fresh Character actor `QA Creator Retry Live`, opened the Character Creator from the locked sheet, and intentionally applied the incomplete initial state. The validation dialog listed skill points 0/10, manifestation points 0/4, missing starting Truth, and fragments 0 instead of 3. Clicking `Review` reopened the creator with the current state preserved, displayed the `Review Needed` alert, and visibly highlighted Step 6 / Final Touches. The corrected retry set Athletics 10, Aegis 4, starting Truth `QA Creator Retry Truth`, and Starting Fragments 3; the apply preview then showed Skill Points 10/10, Manifestation Points 4/4, Attachment Points 5/5, Spark 1, Starting Fragments 3, and the starting Truth. Confirming posted `Updated character creator details`, and the actor sheet showed `QA Creator Retry Truth` under Truths on Page 2. `error.2026-07-15.log` remained 0 bytes after the pass and `npm.cmd run validate` passed.
- 2026-07-15 Player4 browser creator QA created/opened player-owned `QA Player4 Creator Live`, verified the sheet defaulted unlocked/editable, selected Medical -> Therapist Community, The Wanderer, Elemental, Cult of the Saints, one level-5 Bond, 10 Skill points, 4 Manifestation points, Spark 1, Fragments 3, and identity fields. The live browser first exposed a real dependent-select defect: hidden duplicate Archetype options could still be selected programmatically by duplicate values, so creator option values are now parent-scoped and hidden dependent options are disabled. The live run also exposed that cached pre-patch creator application still opened source-granted attachment prompts after the preview; code now passes non-interactive attachment definitions and Dominion defaults, while the live cached run was completed by answering those prompts. Final sheet readback showed `God/dess of QA Smoke Tests`, `Medical - Therapist`, `The Wanderer`, `God/dess of QA Smoke Tests` Dominion, `Cult of the Saints`, Athletics 5, Discipline 5, Medicine 4, Aegis 4, created `QA Player4 Bond`, source-granted attachments, `This is My Town`, `Holes in My Pockets`, and `QA Player4 Starting Truth`. Visual checks found no broken visible images, title boxes contained `Name` and `God/dess Of`, and lined textareas used local line backgrounds with internal scrolling. `error.2026-07-15.log` remained 0 bytes and `npm.cmd run validate` passed.
- 2026-07-15 source visual follow-up tightened printed Character sheet title containment, bounded rich-text/detail image rendering with `object-fit: contain`, and clipped all printed line backgrounds to their padding boxes so ruled sections stay neat while content scrolls. `node --check scripts\validate-release.mjs`, `git diff --check -- styles\part-time-gods.css scripts\validate-release.mjs`, and `npm.cmd run validate` passed.
- 2026-07-15 image/line containment follow-up switched fallback-wired sheet/app images, actor portraits, Pantheon member portraits, and Balance tracker portraits to contained rendering instead of crop rendering; capped printed ruled textareas/lists with local scrolling; and added release-validator guards for these selectors. `node --check scripts\validate-release.mjs`, `git diff --check -- styles\part-time-gods.css scripts\validate-release.mjs`, `rg -n "object-fit: cover|img\[data-fallback-src\]|ptg-print-ability-list|ptg-print-truth-list|max-height: 22rem|max-height: 18rem" styles\part-time-gods.css scripts\validate-release.mjs`, and `npm.cmd run validate` passed; the grep found no remaining `object-fit: cover` sheet portrait rules.
- 2026-07-15 image/table/line polish follow-up routes legacy Domain and Power item templates through shared fallback image context, keeps printed title labels on one neat line beside scrollable value boxes, and bounds rich-text tables, cells, media, and horizontal rules inside their holding boxes. `node --check scripts\validate-release.mjs`, `node --check scripts\check-release-assets.mjs`, `node --check module\sheets\item-sheet.mjs`, `git diff --check -- styles\part-time-gods.css templates\item\domain-sheet.hbs templates\item\power-sheet.hbs scripts\validate-release.mjs`, and `npm.cmd run validate` passed.
- 2026-07-16 image rendering/line follow-up tightened fallback-wired images, profile portraits, rich-text images, printed-page item detail media, horizontal rules, older `ptg-line-field` rows, and ruled textarea/list backgrounds so images remain contained and all ruled lines stay inside their holding boxes. The lined surfaces now use `padding-box` clipping/origin with paint containment, and release validation was updated to guard that behavior. `node --check scripts\validate-release.mjs`, `git diff --check -- styles\part-time-gods.css scripts\validate-release.mjs`, and `npm.cmd run validate` passed.
- 2026-07-16 #131 release packaging update: Foundry was closed cleanly and `npm.cmd run check:release-assets` then reported all declared package pack locks released. `npm.cmd run zip` created `dist\part-time-gods-0.1.0.zip` with 104 files and `dist\system.json`; the follow-up release check reports stale local ZIPs cleared, local ZIP present at 5,812,289 bytes, local manifest version `0.1.0`, and local download URL correct. The gate still fails only on live GitHub assets: latest release `system.json` still advertises `0.0.2`, latest release download still points at `v0.0.2/part-time-gods-0.0.2.zip`, and `v0.1.0/part-time-gods-0.1.0.zip` still returns HTTP 404. Local browser/CLI upload is unavailable in this session, so `.github/workflows/release.yml` now provides the authenticated publication path through GitHub Actions once this state reaches `main` and the `v0.1.0` tag/workflow is run.
- 2026-07-16 #131/#165 manual drag/drop gate is still red by design: `npm.cmd run check:dragdrop-evidence` reports 15 required physical rows, 0 Pass, 13 blank Pending rows, `item-condition` Pending with new Computer Use failure evidence, Armor Partial from the prior runtime `DataTransfer` proof, and `actor-territory-character` Pending. The latest item attempt opened `Part-Time Gods Premade Items -> Conditions` and physically dragged `Afraid` from icon/text to the unlocked Character sheet Conditions panel and sheet body; no owned Condition appeared and no warning appeared, matching the current automation-native-drag limitation.
- 2026-07-15 #182 source slice added a GM-side `Show Player Bar` action to tracked Mortal-Divine Balance character cards, a socket-opened compact player bar that renders only the selected owned character context and meter, owner-only non-GM targeting that does not depend on stale `User.active` state, clear GM feedback when no owning player is available, and automatic refresh for actors the GM has already shown this session. Guardrails now cover the socket/API/template/CSS surface, and focused source tests prove a GM's `actor.isOwner` view does not target observer/non-owning players. `node --check module\apps\mortal-divine-tracker.mjs`, `node --check part-time-gods.js`, `node --check scripts\validate-release.mjs`, and `node --test tests\rules\source-data.test.mjs` passed; the focused test needed the normal unsandboxed rerun after the Foundry data-path `EPERM`.
- 2026-07-16 #182 Computer Use proof re-entered `PTG Migration Snapshot QA` from Setup, joined the Electron host as `Gamemaster`, joined normal Edge as owning `Player3`, and joined a separate Edge InPrivate session as non-owning `Player2`. After closing Player3's existing Mortal-Divine window, the GM `Show Player Bar` button reported `Sent Character's balance bar to Player3.` Player3 then received the compact `Mortal-Divine Balance` window with `GM-shared character balance`, `Character`, `Balanced (-1)`, and the balance meter while exposing no GM roster/action controls. Re-sending with Player2 connected still targeted Player3; Player2 retained only `User Configuration: Player2` and had no Mortal-Divine window, no balance meter, and no GM controls.
- 2026-07-15 migrated-world snapshot QA cloned older local world `tester` (`systemVersion: 0.0.2`) to `ptg-migration-snapshot-qa`, corrected an initial UTF-8 BOM in the cloned `world.json`, started the clone, accepted Foundry's `0.0.2 -> 0.1.0` world migration with backup, and joined as GM. The first migrated-world startup exposed red locked-pack notifications for `part-time-gods.premade-items`; ready-time compendium auto-population now skips protected package packs before any write attempt while the manual GM Setup populate action remains deliberate. Focused source-data tests passed 28/28 outside the sandbox after the expected Foundry data-path `EPERM`, `npm.cmd run validate` passed, and a live F5 reload of the migrated clone showed only the normal blue `Part-Time Gods compendiums are already populated.` message with no delayed red locked-compendium warning. The July 15 error log contains the earlier clone-manifest BOM parse errors from before the cloned `world.json` was re-saved without BOM, so older same-day `0 bytes` notes apply only to their earlier pass windows.
- 2026-07-15 #165 Territory drop follow-up now resolves Actor UUID, Token UUID, Actor ID, and Token-typed ID payloads through `territoryActorFromDropData()` before the GM Territory app decides whether to warn. Focused source-data tests passed 29/29 outside the sandbox after the expected Foundry data-path `EPERM`, syntax passed for `module/apps/territory-grid-app.mjs`, and `npm.cmd run validate` passed. A live migrated-clone F5 reload kept the locked-pack warning fixed, but two visible actor-directory drags from `Character`/`Character (2)` into the Territory grid and points panel still did not deliver a native Foundry payload or change the `3 points` count under automation.
- 2026-07-15 Player4 hard-reload promptless creator retest created/opened player-owned `QA Player4 Promptless Scoped`, verified the sheet was unlocked/editable, selected Medical -> Therapist Community with a scoped occupation career value, The Wanderer, Elemental, Cult of the Saints, one level-5 Bond, full starting budgets, Spark 1, Fragments 3, and identity fields. The apply preview showed the expected selected choices, confirmation posted all four source-choice chat cards, and no `Define Attachments`, Dominion, validation, or other follow-up dialogs appeared after confirmation. Sheet readback showed `God/dess of Promptless Scoped QA`, `Medical - Therapist`, `The Wanderer`, `God/dess of Promptless Scoped QA` Dominion, `Cult of the Saints`, Athletics 6, Discipline 7, Aegis 4, source-granted `Therapist Community`, `I Know a Shortcut`, `Holes in My Pockets`, created `QA Promptless Scoped Bond`, and `QA Promptless Scoped Starting Truth`. `error.2026-07-15.log` remained 0 bytes.
- Browser-side console QA found and fixed the PTG scene-control `SceneControlTool#onClick` deprecation by moving the GM Setup and Territory control tools to `onChange`; after reload, clicking both controls produced no warning/error entries. The Computer Use Electron console audit also fixed repeated PTG `_source` errors from compendium pack updates lacking `_id` and removed the deprecated PTG `renderChatMessage` hook registration. The remaining 8 console warnings are from the separate Easy Target module's deprecated global `Token`/`TokenLayer` access.

## Current Issue Map

| Issue | Status | Evidence | Next completion step |
| --- | --- | --- | --- |
| #181 Critical Failure Effects and Gear Qualities as journals | Closed as completed | `module/data/premade-items.mjs` no longer seeds 11 Critical Failure Effect Conditions or 42 Gear Quality Items. `module/data/premade-journals.mjs` now generates source-backed `Critical Failure Effects`, `Gear Qualities: Armor and General`, and `Gear Qualities: Weapon` rules pages. `module/data/premade-item-audit.mjs`, `scripts/build-compendium-packs.mjs`, `module/data/premade-compendiums.mjs`, `tests/rules/source-data.test.mjs`, and `scripts/validate-release.mjs` reject these rules-reference families as premade Items. Focused source tests and release validation pass with 600 premade Items. The 2026-07-15 copied-pack audit found 0 stale Critical Failure/Gear Quality rules-as-items and the live Electron compendium search returned no Premade Items for `Critical Failure` or `Gear Qualities`; Rules Reference rendered `Critical Failure Effects`, `Gear Qualities: Armor and General`, and `Gear Qualities: Weapon` as journal pages. A GitHub evidence comment was posted to #181, then the issue was closed as completed on 2026-07-15. | Keep release ZIP rebuild under #131/release QA after Foundry releases active LevelDB pack locks. |
| #180 Manifestation Application rules as journals | Closed as completed | `module/data/premade-items.mjs` no longer seeds 27 `manifestation-application` power Items. `module/data/premade-journals.mjs` now generates two source-backed Manifestation Application rules pages under `04. Divine Expressions` with suggested Skills, common Measures, source pages, and explicit rules-reference-not-Item wording. Tests and release validation reject Manifestation Application Item/folder regressions. The 2026-07-15 copied-pack audit found 0 stale Manifestation Application rules-as-items and the live Electron compendium search returned no Premade Items for `Manifestation Applications`; Rules Reference rendered `04. Divine Expressions -> Manifestation Applications: Aegis, Beckon, Journey, Minion, and Oracle` as journal content. A GitHub evidence comment was posted to #180, then the issue was closed as completed on 2026-07-15. | Keep release ZIP rebuild under #131/release QA after Foundry releases active LevelDB pack locks. |
| #179 Character Creator invalid-selection reload | Closed as completed | `module/sheets/character-sheet.mjs` reopens the Creator after reviewable validation failures with the current selections preserved, shows an in-dialog review alert, highlights problem steps/fieldsets, restores prior core choices, Occupation career, Archetype options, budgets, starting attachments, and identity fields, and reports stale/missing source Items plus failed source applications as reviewable `failedChoices`. Missing source Item resolution now fails fast before mutating the actor, and failed source application now happens before creator budget/field writes, so Review retries do not double-add Skill or Manifestation points after a corrected retry. `tests/rules/character-creator.test.mjs` covers problem-step mapping, escaped review HTML, stale source detection, normal apply persistence, missing source no-mutation behavior, failed source application reporting, and corrected retry after Review without doubled budgets. Focused tests pass 10/10, full `npm.cmd test` passes 87/87, and release validation passed. 2026-07-15 Computer Use started from Setup, created `QA Creator Retry Live`, proved invalid creator apply opens Review with preserved state and Step 6 highlight, corrected the preserved values, reached the apply preview, confirmed it, and verified `QA Creator Retry Truth` appeared on the actor sheet. A GitHub evidence comment was posted to #179, then the issue was closed as completed on 2026-07-15. | Keep future creator edge cases under #131 production QA or a new focused issue. |
| #177 D&D5e-like Character sheet mechanics | Closed as completed | `sheet-edit-lock.test.mjs`, `character-sheet-details.test.mjs`, `character-sheet-scroll.test.mjs`, release validation; player-owned sheets now default editable and duplicate header actions are removed. 2026-07-15 D&D5e/PTG source comparison maps D&D5e `_renderModeToggle()`, `_disableFields()`, `.always-interactive` controls, readonly textareas, editable-only config/edit/delete controls, non-edit gameplay controls, favorites/use actions, and collapsible inventory descriptions to PTG `data-ptg-edit-lock-toggle`, allowed locked/read-only controls, readonly textareas, item action gating, roll/resource/Creator actions, and `.ptg-sheet-detail-body` rows. 2026-07-14 Computer Use opened GM-side `QA Character`, confirmed locked `Name` and `God/dess Of` rows stayed inside their boxes, unlocked the sheet, performed and restored a live name edit, then relocked the sheet. Follow-up browser proof joined as true non-GM `Player3`, opened owned Character `Test`, confirmed the sheet defaulted unlocked with 45 enabled inputs, 4 editable textareas, enabled Creator/Edit/Delete controls, and 31 enabled roll buttons; after locking, mutation controls disabled, 4 textareas became readonly, all 31 roll buttons plus 6 detail buttons and Creator remained usable, then the sheet was restored unlocked. Follow-up `sheet-edit-lock.test.mjs` coverage now proves observer and non-owner Character sheets stay read-only even after lock toggles, keep details/tabs usable, block roll/use/edit mutation controls, and leave owner-level player sheets editable by default. 2026-07-15 Browser proof joined as true non-GM `Player4`, reached a rendered `/game` UI, showed only `Player4` and `Gamemaster [GM]` in the player list, showed an empty Actors directory for Player4, clicked a visible `QA Character` Health Damage chat-card action, posted no new chat message, and displayed the expected warning `No editable actors are available for applying damage.` The later Player4-owned creator run opened `QA Player4 Creator Live` as editable/unlocked, completed source-backed character creation, and verified title boxes/lined areas stayed contained after apply. The source visual follow-up now also bounds printed rich/detail images, constrains title boxes, and keeps printed line backgrounds local and clipped while text scrolls. The observer follow-up set Player4 to OBSERVER on `QA Character`, reloaded the Player4 browser client, verified `QA Character` became visible in the Player4 Actors directory without ownership, opened the sheet with the `Read Only` banner, found 0 editable textareas and 4 readonly textareas, 147 disabled buttons, disabled Creator/Edit/Delete/Award/Spend/Use/Battle/Aegis mutation/play-roll controls, enabled Page 1/Page 2 tabs and Details controls, switched to Page 2, expanded an item Details row, and confirmed detail bodies used internal `overflow-y: auto` scrolling. Final 2026-07-15 Computer Use side-by-side proof opened a live D&D5e Character sheet, verified Play/Edit mode, header/sidebar readouts, grouped embedded Feature controls, and expanded `Martial Arts` details, then returned to PTG, opened `QA Character`, verified locked/unlocked mode, performed and restored a live name edit, navigated Page 2 embedded sections, expanded `Borrowed Lives` Details with internal rich-text scrolling, and scrolled through Bonds, Worshippers/Vassals, Relics, Truths, and Gear/Notes with controls and ruled sections contained. `error.2026-07-15.log` stayed 0 bytes. GitHub evidence comments were posted to #177 as issue comments `4984627251`, `4985098942`, and `4985544256`; #177 was closed as completed on 2026-07-15. | Keep future Character sheet regressions under #131 production QA or a new focused issue. |
| #176 Territory scene image/background/GM controls | Closed as completed | `source-data.test.mjs` covers overlay fit, background updates, scene update failure feedback, v14 FilePicker-backed user-data browsing, character import, and player-facing data separation. 2026-07-14 Computer Use verified the GM Territory app renders, scrolls internally in both directions, resizes without label crush, opens the Background dialog readably, and `View` frames the player-facing scene/grid overlay. The same pass used the GM Import control to convert 4 `QA Character` territory-ready attachments into grid points, found narrow-cell point names wrapping vertically, then hard-reloaded after the CSS fix and rechecked horizontal ellipsized chips with full point names still readable in the side list. The post-fix live check opened the GM interface, repaired Legend to `x=8,y=8,w=84,h=42`, kept 24 managed overlay drawings at sort `5000..5023`, applied background image `icons/weapons/swords/sword-broad-serrated-blue.webp` plus color `#112233` through v14 `levels[0].background`, restored the previous background, and captured 0 PTG console errors or warnings. A separate headless Edge client logged in as role-1 `Player3` and proved `game.partTimeGods.openTerritoryInterface()` routes non-GM users to the `God Territory Grid` scene, not the GM app; the player canvas showed `Scene.vZ5MWDgsubN90MZW`, board `756x454`, fit scale `0.325`, and no `.ptg-territory-grid-window`. A GitHub evidence comment was posted to #176, then the issue was closed as completed. | Keep hardware-level actor drag under #165/#131 because it is not part of #176's scene image/background/overlay/legend acceptance criteria. |
| #174 Divine identity generator and character creator | Closed as completed | `random-god-generator.test.mjs`, `character-creator.test.mjs`; creator has manual identity fields, apply-only random identity, source-influenced generator, backed starting attachment items, and direct apply-persistence coverage for identity fields, Skills, Manifestations, Spark, Fragments, starting Truth, starting attachments, and selected source Items. 2026-07-14 Computer Use stepped through all six creator pages, generated Random God suggestion `Aurel of the Borrowed Crown, God/dess of Light`, applied the identity suggestion into editable fields, completed a valid 10 Skill / 4 Manifestation / 5 Attachment build, confirmed the apply dialog, saw expected non-overwrite warnings for already-set Archetype/Dominion/Theology, verified Page 1 persisted `God/dess of Light`, and verified Page 2 created level-5 owned Bond `QA Creator Live Bond`. A GitHub evidence comment was posted to #174, then the issue was closed as completed. | Keep optional no-choice/one-choice live variant spot checks under #131 breadth QA |
| #172 Premade Item/Journals audit | Closed as completed | `source-data.test.mjs`, `validate-release.mjs`, and `scripts/audit-premade-items.mjs`; Battle of Fists/Wits action rules were moved out of Items and into rules journals, and the follow-up #180/#181 source conversion reduced premade Items to 600 while rejecting Manifestation Application, Critical Failure Effect, and Gear Quality rules-reference Items. The current item audit reports 640 created Items across character creation and premade item sources with 0 invalid types, invalid folders, duplicate type/name pairs, duplicate source IDs, journal-style Items, `journal:` source IDs, or missing source IDs. Rules Reference has 9 JournalEntries and 56 pages without repeated placeholder text. 2026-07-14 Computer Use verified live Premade Items no longer shows retired Battle folders/search results and `Rules Reference -> 06. Divine Battles` includes Battle of Fists/Wits journal pages; opened `Rules Reference -> 04. Divine Expressions -> Rituals and Otherworlds` and verified Otherworld travel is procedure content; live Premade Items search for `Otherworld` returned only legitimate Truth `Otherworldly Sight`; live Character Creation pack exposed expected source choice folders and representative `Medical` Occupation plus `Ascendants` Theology Items. A GitHub evidence comment was posted to #172, then the issue was closed as completed. | Keep #180/#181 pack rebuild/live compendium proof on their focused issues, and future breadth checks under #131 production QA. |
| #171 Native Foundry combat flow | Closed as completed | `ptg-combat.test.mjs` covers native combatant initiative, tracker formula, action markers, healing, Health/Psyche damage, and GM restrictions. 2026-07-14 Computer Use created an encounter through the native Combat Tracker, added `QA Character` and `QA Antagonist` through token HUD combat controls, rolled tracker initiative to `QA Antagonist 10` / `QA Character 6`, then used `PTG GM Setup -> Combat Controls` to post Quick Action, Health damage `7 -> 6`, Psyche damage `8 -> 7`, Health healing `6 -> 7`, and Psyche healing `7 -> 8` for `QA Character`. A final GitHub evidence comment was posted to #171, then the issue was closed as completed. | Keep any broader combat-adjacent production QA edge cases under #131 |
| #170 Mortal-Divinity Balance Tracker resize/scroll | Closed as completed | `source-data.test.mjs`, release validation stylesheet guards for GM/player tracker sizing and scrolling; `balanceActorFromDropData` now covers Actor UUID, Token UUID, Actor ID, and invalid drop data for GM roster import. 2026-07-14 Computer Use verified the GM tracker resized, kept roster controls readable, and scrolled internally to Custom Adjustment/Recent Movement. The follow-up runtime `DataTransfer` drop in the live Electron client switched the tracker from `QA Character` to `Test`, proving the GM roster drop handler executes in Foundry. A headless Edge client logged in as role-1 `Player3` with assigned character `Test`; the player Balance tracker rendered `Your Mortal-Divine balance`, showed `Test`, did not show `QA Character`, and did not expose Add Character, Remove, or Custom Adjustment controls. The compact player proof then caught and fixed the legacy `ptg-balance-tracker` module overwriting `game.partTimeGods.openMortalDivineBalanceTracker`; after the system reasserted its integrated API, `game.partTimeGods` and `game.ptg.balance` used the same player-safe opener, the player app returned normally, rendered a marked player view at `420x360`, and produced 0 console errors/warnings. A GitHub evidence comment was posted to #170, then the issue was closed as completed. | Keep any future tracker drag/drop edge cases under #131 or a new focused issue |
| #168 All Item sheets resize/scroll/media | Closed as completed | `character-sheet-details.test.mjs`, release validation stylesheet/template/media guards; item sheets use fallback images and scrollable rich text surfaces. 2026-07-14 Computer Use live sweep opened QA Items for all registered Item types: Occupation, Archetype, Domain, Theology, Power, Attachment, Bond, Truth, Relic, Worshipper, Vassal, Blessing, Curse, Condition, Gear Quality, Weapon, and Armor. Every sheet was measured at compact `520x520`, rendered an internal `486x450` scroll body with `overflow-y: auto`, reported no bad controls, labels, or editor surfaces, and missing artwork fell back to `icons/svg/item-bag.svg` with a loaded `naturalWidth 512`. Short-content valid-art checks proved representative Weapon and Occupation images render directly. A GitHub evidence comment was posted to #168, then the issue was closed as completed. | Keep future item-sheet visual regressions under #131 production breadth QA or a new focused issue |
| #182 GM send owned Mortal-Divine bar to player | Closed as completed | `module/apps/mortal-divine-tracker.mjs` now registers a system socket, exposes `showMortalDivineBalanceBarToOwners()`, targets non-GM owner users through exported `balanceBarOwnerUsers()` without depending on stale active flags, and opens a compact `MortalDivineBalancePlayerBar` for targeted clients. `templates/apps/mortal-divine-tracker.hbs` adds `data-balance-show-player` to GM tracked-character cards and renders a separate `data-balance-player-bar` view with only character name/context and the balance meter. `styles/part-time-gods.css`, `lang/en.json`, `part-time-gods.js`, `scripts/validate-release.mjs`, and `tests/rules/source-data.test.mjs` cover the UI/API/localization/validation surface and owner-only targeting edge case. Focused source-data tests pass 31/31 outside the sandbox after the expected Foundry data-path `EPERM`. Live migrated-world browser proof confirmed Player3 owns `Character` and the GM `Show Player Bar` action reports `Sent Character's balance bar to Player3.` The live pass also fixed the delegated preset-action bug caused by boolean `data-balance-action`. 2026-07-16 Computer Use then proved the true separate-client flow: Electron GM sent the bar, normal Edge Player3 received the compact player-only bar, and simultaneous Edge InPrivate Player2 received no bar or GM controls. GitHub evidence comment `4993637128` was posted, and #182 was closed as completed on 2026-07-16. | Keep future Balance tracker breadth under #131 or a new focused issue. |
| #165 Unified Territory scene module | In progress, source/test covered for core data model, scene helpers, macro/API replacement review, and Actor/Token drop payload resolution; GM and player route live-proven | `source-data.test.mjs`; territory points separate GM/player data, player character import creates points, overlay fits canvas, background controls update scenes, and the background browser resolves Foundry v14 `FilePicker.implementation`. Scene-control buttons now use Foundry's v14/v15-safe `onChange` callback and were rechecked live with no console warning. `part-time-gods.js` now has explicit source coverage that the integrated `game.ptg.territory` API is the primary entry point, gets restored after competing module overrides, auto-opens through `maybeOpenTerritoryInterfaceOnReady()`, and that `PTG: Create Territory Scene` plus `PTG: Territory Controls` remain compatibility launchers pointing to the native Unified Territory interface rather than macro-first workflow. `territoryActorFromDropData()` now resolves Actor UUID, Token UUID, Actor ID, and Token-typed ID payloads before the GM app warns, matching the broader Foundry drag shapes already supported by the Mortal-Divine tracker. 2026-07-14 Computer Use proved the GM Import control path against `QA Character`, creating 4 points and 2 influenced cells, and the compact-cell text fix was visually rechecked after hard reload. The follow-up drop patch/runtime proof confirmed live Territory drop-handler execution with Foundry `DataTransfer` payloads. The post-fix Computer Use pass proved GM open repairs stale scene overlay geometry, keeps the overlay foregrounded, applies/restores v14 level-backed background image/color, and leaves the PTG console clean. The headless `Player3` client proved non-GM `openTerritoryInterface()` opens only the Territory scene/canvas and not the GM interface. 2026-07-15 Computer Use re-entered the PTG QA world from Setup as GM and confirmed the integrated Territory Grid and Mortal-Divine interfaces auto-opened immediately over the Territory scene; the actor directory opened beside the GM Territory window, the imported `QA Character` Territory data still showed 4 points and 2 influenced cells, and two actor-directory drag attempts from `QA Player4 Promptless Scoped` into the GM Territory side panel/grid body did not deliver a native Foundry Actor payload or change the 4-point count. A later migrated-clone F5 retest kept the app open with 3 points and 2 influenced cells, but actor-directory drags from `Character` and `Character (2)` into the grid/panel likewise did not deliver a native payload or change the 3-point count under automation. `docs/qa/manual-dragdrop-matrix.md` now tracks the remaining `actor-territory-character` proof row, and `npm.cmd run check:dragdrop-evidence` keeps it failing until marked Pass. GitHub evidence comments `4985892336` and `4986083828` were posted to #165. | Manual/hardware-level Actor drag into the GM Territory interface remains the only #165 closure gate because Computer Use still cannot prove the native drag payload. |
| #131 Foundry v14 production QA | Open, partially live-proven with repaired compendia, true non-GM player-client proof, and migrated-world proof | `docs/qa/v14-production-smoke-test.md` exists and automated validators pass; 2026-07-14 Computer Use verified the QA world loads in Electron, character creator/sheet basics work, locked/unlocked Character sheet editing works with title text contained, GM apps render/resize/scroll, Territory Import creates character-derived points without vertical label clipping after the CSS fix, runtime `DataTransfer` drops reach Territory and Balance handlers, runtime Item drop creates owned Armor on `QA Character`, equipped Armor plus Fire Proof reduces fire Health damage, all registered Item sheets resize/scroll with fallback media, repaired compendia match source, native tracker initiative works, Combat Controls can post action/damage/healing cards, Creator opens from a locked sheet with an internally scrolling six-step dialog, Territory background/legend/foreground overlay passes, true player permission slices are live-proven, and the post-fix Electron console/runtime logs captured 0 PTG errors/warnings. 2026-07-15 source recheck covers Foundry drop-data parsing, direct Character sheet Item drops for the #131 matrix including `power` and `gearQuality`, typed-section mismatch rejection, Attachment of Choice conversion, legacy embedded-item migration, and explicit migration skip/force behavior. The Player4-owned creator browser runs proved a non-GM player can complete source-backed creation and persist choices/items without extra prompts. The source visual follow-ups add release-validated image containment, shared fallback rendering for the legacy Domain/Power item templates, bounded rich-text table/media rendering, and neat local ruled-line behavior. The local release packaging path is now clean: Foundry pack locks were released, stale local ZIPs were removed, and `dist\part-time-gods-0.1.0.zip` plus `dist\system.json` were built. The install gate remains open because live GitHub Release assets are stale/missing, and `check-release-assets.mjs` records that remote failure directly. `docs/qa/manual-dragdrop-matrix.md` and `npm.cmd run check:dragdrop-evidence` now track the remaining physical item drag/drop rows and keep the gate failing until all `item-*` rows are Pass. The Player4 observer proof and migrated-world snapshot proof are complete. GitHub evidence comments were posted to #131 as issue comments `4984628687`, `4985099020`, `4985357675`, `4985798606`, `4985942160`, `4985994530`, `4986040319`, and `4986082543`. | Complete physical item drag/drop breadth and install-by-manifest after release assets. |

## Next Execution Order

1. Complete the physical drag/drop pass from `docs/qa/manual-dragdrop-matrix.md` using a hardware/native Foundry drag path, update every row with evidence, and rerun `npm.cmd run check:dragdrop-evidence`.
2. Continue #165 integrated Territory follow-through through the `actor-territory-character` row; Computer Use has now failed to deliver native Actor and Item drag payloads, so this row needs a true manual drag or another native-payload proof path.
3. Continue #131 install-by-manifest after release assets: merge/push the verified release state to `main`, run the `Publish Release Assets` workflow by pushing tag `v0.1.0` or dispatching it from `main`, rerun `npm.cmd run check:release-assets`, then use a clean Foundry install target to install from the live manifest URL.
